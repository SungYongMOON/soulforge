# .unit/guild_master/artifacts

- `artifacts/` 는 guild-master lane 에서 public-safe 로 남길 수 있는 canonical artifact guidance 만 둔다.
- 실제 authoring 산출물과 install 결과는 `_workmeta/<project_code>/runs/<run_id>/` 아래에 남긴다.

