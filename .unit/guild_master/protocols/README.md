# .unit/guild_master/protocols

- `protocols/` 는 skill package review, promotion, install handoff 절차 메모를 둔다.
- 실제 실행 transcript 와 raw coordination log 는 `_workmeta/<project_code>/runs/<run_id>/` 아래에 남긴다.

