# .unit/guild_master_01

- `guild_master_01/` 는 skill package authoring, boundary review, promotion handoff 성향의 active subject sample 을 둔다.
- `unit.yaml` 은 administrator owner lens 를 `summary`, `identity`, `class_ids` 중심으로 고정한다.
- 이 경로의 tracked sample 은 owner surface 만 설명하고 runtime truth 는 담지 않는다.
