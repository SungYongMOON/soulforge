# .unit/forge_packager/memory

- `memory/` 는 `forge_packager` unit 이 장기적으로 유지할 public-safe operating memory baseline 을 설명하는 surface 다.
- private transcript, protected packet detail, raw dump 는 여기에 두지 않는다.
