# .unit/vein_investigator/runtime

- `runtime/` 는 `vein_investigator` unit 의 public-safe runtime note baseline 을 설명하는 surface 다.
- live run dump, private event stream, protected output path 는 여기에 두지 않는다.
