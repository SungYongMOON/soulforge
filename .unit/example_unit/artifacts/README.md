# artifacts

- 무엇을 둔다: unit-owned sanitized artifact shelf template.
- 무엇을 두지 않는다: project-local raw output, run-scoped evidence, battle log attachment.
- 왜 이렇게 둔다: artifacts 는 재사용 가능한 unit 산출물만 두고, run truth 는 `_workspaces/` 에 남긴다.
