# .unit/scribe_01

- `scribe_01/` 는 research and documentation 성향의 contrasting active subject sample 을 둔다.
- `unit.yaml` 은 evidence-first owner lens 를 `summary`, `identity`, `class_ids` 중심으로 고정한다.
- 이 경로의 tracked sample 은 owner surface 만 설명하고 runtime truth 는 담지 않는다.
