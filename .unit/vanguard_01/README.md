# .unit/vanguard_01

- `vanguard_01/` 는 canonical active subject sample 을 둔다.
- `unit.yaml` 은 현재 active subject shape 를 `summary`, `identity`, `class_ids` 중심으로 고정한다.
- 이 경로의 tracked sample 은 owner surface 만 설명하고 runtime truth 는 담지 않는다.
