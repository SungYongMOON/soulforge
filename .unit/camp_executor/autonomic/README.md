# .unit/camp_executor/autonomic

- `autonomic/` 는 `camp_executor` unit 의 self-check, follow-through automation hint, bounded preparation rule 을 설명하는 surface 다.
- live runtime state 나 high-churn logs 는 여기에 두지 않는다.
