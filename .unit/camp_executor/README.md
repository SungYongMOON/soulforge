# .unit/camp_executor

- `camp_executor/` 는 회의 후속의 담당, 기한, 요청 발송, 일정 반영 압박을 맡는 오크 `집행관` trial unit 을 둔다.
- 이 unit 은 owner/date chase, outbound request follow-through, schedule pressure 성향의 owner lens 를 가진다.
- `unit.yaml` 은 current trial shape 를 `summary`, `identity`, `class_ids` 중심으로 고정한다.
- 이 경로의 tracked sample 은 owner surface 만 설명하고 runtime truth 는 담지 않는다.
