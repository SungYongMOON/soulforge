# .unit/camp_executor/protocols

- `protocols/` 는 `camp_executor` unit 이 반복적으로 따를 execution follow-through sequence 와 handoff rule 을 설명하는 surface 다.
- project-local action body 자체는 protocol 설명과 분리한다.
