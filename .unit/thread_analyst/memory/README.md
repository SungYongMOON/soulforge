# .unit/thread_analyst/memory

- `memory/` 는 `thread_analyst` unit 이 장기적으로 유지할 public-safe operating memory baseline 을 설명하는 surface 다.
- private transcript, protected analysis body, raw dump 는 여기에 두지 않는다.
