# .unit/thread_analyst

- `thread_analyst/` 는 회의 맥락 stitching 과 문서 반영 후보 추출을 맡는 엘프 `기록관` trial unit 을 둔다.
- 이 unit 은 long-context stitching, document-impact extraction, evidence-backed synthesis 성향의 owner lens 를 가진다.
- `unit.yaml` 은 current trial shape 를 `summary`, `identity`, `class_ids` 중심으로 고정한다.
- 이 경로의 tracked sample 은 owner surface 만 설명하고 runtime truth 는 담지 않는다.
