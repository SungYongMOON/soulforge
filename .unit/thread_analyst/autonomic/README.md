# .unit/thread_analyst/autonomic

- `autonomic/` 는 `thread_analyst` unit 의 self-check, context-stitch hint, bounded automation idea 를 설명하는 surface 다.
- live runtime state 나 high-churn logs 는 여기에 두지 않는다.
