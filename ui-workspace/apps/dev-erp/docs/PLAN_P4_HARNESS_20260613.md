# 실행 계획: A) P4 게이트+생성기 × B) 하네스 강화 (계획만 — 빌드 0)

- 작성: 2026-06-13, claude_fable-5 (run18, 코드/화면/DB 변경 없음)
- 전제: run17까지 완료(과제 허브, P2a 할일 쓰기, 메일 분류). 가드레일
  유지(메타만/외부전송 0/DB gitignore/localhost).
- 기존 자산 앵커: guild_hall/{doctor,validate,daily_ledger}/ 실존,
  후보큐에 `agent_boot_digest_v0`(proposed, owner 보류 결정 1건 내장),
  `doctor_platform_binary_check_v0`(proposed),
  `daily_work_ledger_automation_v0`(**completed** — 업무일지 데이터 레인 존재),
  `candidate_queue_archive_policy_v0`(proposed).

규모 기준: 소=반나절 내, 중=1~1.5일, 대=2일+. (강한 모델 윈도우 기준)

---

## A) P4 — 단계 게이트 강제 + 생성기 (앱 안 첫 LLM 연동)

| # | 슬라이스 | 한 줄 | 의존 | 규모 | 위험 | self-verify |
| --- | --- | --- | --- | --- | --- | --- |
| A1 | 게이트 판정(읽기) | 가이드 final(Out) 스텝+필수 할일로 단계 통과 가능 여부 계산 → "단계 게이트" 모듈을 실화면으로 | 없음(가이드·할일 기존) | 중 | 소 (읽기 전용) | 판정 함수 node:test + 브라우저 게이트 화면 |
| A2 | 게이트 강제(쓰기) | 게이트 통과/반려 액션, 미충족 시 차단 — **수위는 갈림길②** | A1 | 소~중 | 중 (실업무 방해 가능 → soft 권장) | 통과/반려/강제차단 전이 테스트 + event_log gate_* 확인 |
| A3 | LLM 어댑터 계층 | /api/generate 한 곳에만 LLM. 키는 로컬 env, **ERP는 LLM 없이 100% 동작**(P0) — 갈림길① 결정 필요 | 없음 (병행 가능) | 중 | **대** (데이터 경계) | mock 어댑터 단테스트, 실호출 1회는 owner 입회 |
| A4a | 업무일지 생성기(템플릿) | event_log(project_ref)+guild_hall daily_ledger 레인 → 일일 업무일지 초안 (LLM 불필요, 결정적 조립) | A1과 독립 | 중 | 소 | 고정 fixture→스냅샷 테스트 + 실데이터 미리보기 |
| A4b | 업무일지 LLM 다듬기 | A4a 초안을 어댑터로 문장화(편집자P식: 자동 초안+사람 확정) | A3, A4a | 소 | 중 (갈림길① 종속) | 동일 입력 재실행 일관성 + 사람 확정 단계 강제 확인 |
| A5 | 보고서/연구노트 생성기 | 저장소 기존 skill(.registry)+`.mission` 재사용해 앱에서 트리거, 산출물은 SE 규칙 연동 — 출력 위치는 갈림길③ | A4 패턴 확립 후 | 중~대 | 중 (실파일 생성 시) | dry-run 모드 비교 + Out/Work 규칙 준수 검사 |
| A6 | 보스전=게이트 시각 통합 | 보스 HP=미완 필수 산출물 수, 통과=처치 연출 (P-G 본격 아님, 표시 계층만) | A1·A2 | 소 | 소 | 파리티 테스트 + 양 모드 브라우저 |

A 슬라이스 공통: 모든 쓰기는 event_log(used_refs+data_label+project_ref),
LLM 사용 사건은 used_refs 에 모델/스킬 ref 기록(라벨링 우선 원칙 — 추후
학습/비용 분석 연료).

## B) 하네스 강화

| # | 슬라이스 | 한 줄 | 의존 | 규모 | 위험 | self-verify |
| --- | --- | --- | --- | --- | --- | --- |
| B1 | 검증 게이트 표준화 | 페이즈 종료 체크(테스트/콘솔0/가드레일/문서/packet)를 `tools/verify_gate.mjs` 한 방으로 — 계약 Level 0–3 매핑 | 없음 | 소 | 소 | 게이트로 게이트 자신 검증(통과/고의 실패 2케이스) |
| B2 | doctor 확장 | guild_hall/doctor 에 dev-erp 체크(노드≥22.5/DB 스키마/real_meta 신선도/서버 생존/브랜치 위치/gitignore) + 기존 후보 `doctor_platform_binary_check_v0` 흡수 | 없음 | 소~중 | 소 | 정상/각 고장 시나리오 모의 |
| B3 | boot digest | 필독 체인(1,300줄+)을 100줄 다이제스트+드리프트 가드로 — 기존 후보 `agent_boot_digest_v0` 실행. **routing surface 여부 = 갈림길④(이미 packet에 기록된 owner 보류)** | B1 (드리프트 가드가 게이트에 걸리게) | 중 | 중 (지시 우선순위 체인 접촉) | 다이제스트↔원본 드리프트 테스트 + 신선도 체크 |
| B4 | 후보큐 자동화 | 후보큐 상태 집계(proposed/approved/완료)+owner 승인 대기 보드+archive 정책(`candidate_queue_archive_policy_v0` 흡수). 노출 위치는 갈림길⑤ | B1 | 중 | 소 | 더미 큐 fixture 집계 테스트 |
| B5 | 라벨 감사기 (추가 제안) | event_log 의 used_refs/data_label/project_ref 커버리지 리포트 — 라벨링 우선 원칙 감시(쓰레기 누적 조기 발견) | B1 | 소 | 소 | 결손 주입 후 검출 확인 |
| B6 | inspector 자동 결합 (추가 제안) | 페이즈 커밋 후 독립 inspector 패스를 B1 게이트의 마지막 단계로 규약화 | B1 | 중 | 소 | 과거 run 1개 재검증으로 캘리브레이션 |

## 권장 타임라인 (~10일, 슬라이스 교차)

원칙: **하네스 소형 2개를 최선두**(이후 모든 슬라이스가 그 게이트로 검증됨
— 즉시 회수되는 투자), A는 게이트(읽기→강제) 먼저, 생성기는 LLM 갈림길
답변과 무관하게 템플릿 모드(A4a)로 진행 가능하게 배치.

| 일차 | 슬라이스 | 비고 |
| --- | --- | --- |
| D1 | B2 doctor + B1 verify_gate | 이후 전 슬라이스의 검증 인프라 |
| D2 | A1 게이트 판정+화면 | 이날부터 매일 B1 게이트로 종료 검증 |
| D3 | A2 게이트 강제 + A6 보스 연출 | 갈림길② soft/hard — 기본 soft 로 진행 가능 |
| D4 | B3 boot digest | 갈림길④ 답 있으면 routing 반영, 없으면 companion 모드 |
| D5 | A3 LLM 어댑터 | **갈림길① 답 필요** — 없으면 A4a 와 순서 스왑 |
| D6 | A4a 업무일지(템플릿) → A4b LLM 다듬기 | A4b 는 A3 완료 시에만 |
| D7 | A5 보고서/연구노트 생성기 (전반) | 갈림길③ 출력 위치 |
| D8 | A5 마무리 + B5 라벨 감사기 | |
| D9 | B4 후보큐 자동화 + B6 inspector 결합 | |
| D10 | 버퍼 + 전 슬라이스 게이트 재실행 + owner 데모 정리 | 통합 검증일 |

의존성상 반드시 먼저: **B1+B2**(전체 검증 인프라) → **A1**(게이트 계열의
기반). A3 는 갈림길① 답이 D5 전에 오면 제 위치, 안 오면 A4a/A5(템플릿·
dry-run 한정)와 스왑해 일정 소실 없음.

## owner 결정 갈림길

| # | 질문 | 차단 여부 | 권장 |
| --- | --- | --- | --- |
| ① | **LLM 데이터 경계**: "외부 전송 0" 가드레일과 클라우드 LLM 호출의 충돌 해소 — (a) 사내 tool_pc 로컬 모델만 (b) 구독 클라우드 LLM에 메타(제목 수준)까지 허용 (c) 합성/수기 입력만 | **D5 전 답 필요** (유일한 차단) | 단기 (c)→중기 (a): tool_pc 이전 계획과 정합 |
| ② | 게이트 강제 수위: hard(미충족 통과 불가) vs soft(경고+사유 기록 후 통과) | 비차단 (기본 soft 로 진행) | soft 시작, 데이터 쌓인 뒤 hard 전환 검토 |
| ③ | 생성물 출력 위치: 앱 미리보기+클립보드 / SE 폴더 실파일(Out·Work 규칙) / artifact 포인터만 | 비차단 (미리보기로 시작) | 미리보기→owner 확정 후 실파일 단계 도입 |
| ④ | boot digest 를 AGENTS.md 가 가리키는 공식 routing surface 로 승격할지 (packet 기록된 기존 보류) | 비차단 (companion 모드로 진행) | 1주 companion 운용 후 승격 판단 |
| ⑤ | 후보큐 보드 노출: dev-erp '개발요청함' 모듈로 흡수(앱 안) vs 스크립트 리포트 | 비차단 | 앱 흡수 (P1.5 개발요청함과 합류) |

## 범위 밖 (이번 10일에 안 함)

공유 마스터(품목/재고/거래처/구매 P2b·P3), P-G 본격(점수/랭킹), 팀 공개
+RBAC 강제, Smartsheet 동기화(P5), tool_pc 이전 자체.
