# P-18 — Smartsheet 임베드(read·write 0) — store embed_view 메타 테이블 + /api/embeds + app.js iframe 뷰

- **phase**: UI
- **depends_on**: 없음
- **parallel_group**: backend-export
- **owner_decision**: none (기본=read 임베드만, smartsheet.com 화이트리스트, write/토큰 cut).

## allowed_write_paths
- /Volumes/OPENCLAW_WS/Soulforge/ui-workspace/apps/dev-erp/src/store.mjs
- /Volumes/OPENCLAW_WS/Soulforge/ui-workspace/apps/dev-erp/server.mjs
- /Volumes/OPENCLAW_WS/Soulforge/ui-workspace/apps/dev-erp/static/app.js
- /Volumes/OPENCLAW_WS/Soulforge/ui-workspace/apps/dev-erp/src/lexicon.mjs
- /Volumes/OPENCLAW_WS/Soulforge/ui-workspace/apps/dev-erp/test/core.test.mjs

## summary
Smartsheet 임베드 read-only: owner 가 등록한 published Smartsheet 임베드 URL(메타)을 저장·조회하고 iframe 으로 read 표시. write/양방향·API 토큰 cut(defer). store.mjs embed_view 테이블(IF NOT EXISTS) + upsertEmbed/listEmbeds. server.mjs GET/POST /api/embeds. app.js 'mod:embeds' renderEmbeds(): URL 등록 폼 + iframe 목록. URL 화이트리스트(https + smartsheet 도메인)만 허용.

## data_model
- CREATE TABLE IF NOT EXISTS embed_view ( id TEXT PRIMARY KEY, kind TEXT NOT NULL DEFAULT 'smartsheet', title TEXT NOT NULL, url TEXT NOT NULL, project_id TEXT REFERENCES core_project(id), data_label TEXT NOT NULL DEFAULT 'real', created_at TEXT ); — DDL 상수에 IF NOT EXISTS(멱등). ALTER 불필요.

## code_changes
- src/store.mjs DDL 상수: embed_view CREATE TABLE IF NOT EXISTS 추가.
- src/store.mjs upsertEmbed({id,kind='smartsheet',title,url,project_id=null,data_label='real'}): title/url 필수, url 이 /^https:\/\/(app\.|publish\.)?smartsheet\.com\//.test(url) 아니면 {error:'url_not_allowed'}; 빈 url/title 이면 {error:'title_url_required'}; id=id||`emb_${randomBytes(5).toString('hex')}`; INSERT ... ON CONFLICT(id) DO UPDATE SET title,url,project_id; created_at=now; 반환 {ok:true,id}.
- src/store.mjs listEmbeds({project}={}) → project 있으면 WHERE project_id=? else 전체, SELECT * ORDER BY created_at DESC.
- server.mjs: 'if (path==="/api/embeds" && req.method==="GET") return send(res,200,store.listEmbeds({ project: qp.project }));' 와 POST 분기(body JSON.parse→store.upsertEmbed(...); 에러 시 400; 성공 시 store.appendEvent({ actor_ref:"owner", actor_kind:"human", kind:"embed_register", to:r.id, used_refs:["embed_view"], data_label:"meta" }) 후 send 200).
- static/app.js render(): 'if (state.view==="mod:embeds"){ $("#viewTitle").textContent=L.embed_title; logView(state.view); return renderEmbeds(); }'; 진입은 gates/홈 '외부 보기 열기' 버튼(state.view='mod:embeds') — NAV_LAYOUT 미편입.
- static/app.js 신규 async renderEmbeds(): const embeds=await api('/api/embeds'); URL 등록 폼(title·url+등록→post('/api/embeds',{title,url}).then(r=>r.json())→error url_not_allowed 면 인라인 경고) + 각 embed 를 '<iframe src="${esc(e.url)}" sandbox="allow-scripts allow-same-origin allow-popups" loading="lazy">' read 표시(높이 고정), 빈 목록 L.embed_empty.
- src/lexicon.mjs business+fantasy 양쪽: embed_title(Smartsheet 보기/외부 점술판), embed_add(임베드 등록/점술판 봉입), embed_url(Smartsheet 게시 URL/점술판 표식), embed_url_bad(Smartsheet 게시 URL 만 허용됩니다/허락된 점술판만 가능), embed_open(외부 보기 열기/점술판 폴치기), embed_empty(등록된 임베드 없음/봉입된 점술판 없음).

## ui_changes
- renderEmbeds(): title·url 등록 폼 + 화이트리스트 위반 인라인 경고. 등록된 Smartsheet iframe 목록(read-only, sandbox). gates/홈 '외부 보기 열기' 진입.

## test_cases
- TEST-003 lexicon parity: embed_* 6키 양쪽 일치.
- 신규 node:test 'P-18: embed URL 화이트리스트': freshStore(); assert.equal(store.upsertEmbed({title:'일정',url:'https://app.smartsheet.com/b/publish?x=1'}).ok,true); assert.equal(store.upsertEmbed({title:'bad',url:'https://evil.example.com/x'}).error,'url_not_allowed'); assert.equal(store.upsertEmbed({title:'no-url',url:''}).error,'title_url_required').
- 신규 node:test 'P-18: listEmbeds 조회·project 필터': freshStore()+loadFixture(store); store.upsertEmbed({title:'a',url:'https://app.smartsheet.com/b/1',project_id:'PRJ-A'}); store.upsertEmbed({title:'b',url:'https://app.smartsheet.com/b/2'}); assert.equal(store.listEmbeds({}).length,2); assert.equal(store.listEmbeds({project:'PRJ-A'}).length,1).
- 신규 node:test 'P-18: embed_view DDL 멱등': openStore(':memory:') 2회 각각 스키마 생성 에러 0; 같은 store 같은 id upsert 2회 에러 0(ON CONFLICT).

## acceptance_checks
- node --test 전건 green.
- node server.mjs --db :memory: 기동 후 외부 보기 화면→smartsheet 게시 URL 등록→iframe read 표시(수동 verify), 비-smartsheet URL 거부 경고.
- write/저장 버튼이 iframe 안에 노출돼도 앱은 read 만(외부 동기 코드 0) — event_log embed_register(meta) 만 기록.

## stop_conditions
Smartsheet API 토큰·write·CSV import 양방향 요구 시 즉시 중단(defer). iframe CSP/X-Frame-Options 로 published URL 안 뜨면 '외부 새 탭 열기' 링크로 폴백(임베드 강제 안 함).

## guards
- ① zero-dependency(iframe·메타 테이블만, 외부 API 호출 0)
- ③ 원문 미저장(URL 메타만, secret·토큰 미접촉)
- ④ event_log append-only(embed_register data_label=meta)
- ⑤ ALTER 불필요·DDL IF NOT EXISTS 멱등
- ② 코어 LLM 0%
- ⑩ public-safe
- ⑪ lexicon parity
- ⑧ node:test 전건 green + 표기 commit+push
