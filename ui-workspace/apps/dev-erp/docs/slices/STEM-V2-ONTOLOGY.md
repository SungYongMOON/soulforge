# STEM-V2-ONTOLOGY — 줄기 v2 온톨로지 정본 (골격·작업·이력줄기)

- status: **정본 확정 (2026-07-05 owner 공동설계, ERP 표면 스레드 기록)**
- 소유: 이 문서가 줄기(project_context)의 개념 정본. 생성기 구현은 `ENGINE-10-STEM-V2-GENERATOR.md`,
  reattach API 는 `B6-STEM-REATTACH-API.md` 가 소유.
- 배경: 2026-07-05 owner 가 P24-049 줄기 그래프에서 "제목 문자열 무더기" 문제를 지적 —
  가지가 메일 제목 조각이고, 실무협의 91통이 한 덩어리이며, 상세 행 100개가 동일 라벨.
  원인 실측: 가지 배치가 규칙 키워드→일반 시드→폴백(제목)의 문자열 매칭뿐(haengbogwan_run.mjs
  branchHintForProject)이고 P24-049 는 context_hint_rules 자체가 없음. **의미(탄생·생애) 계층 부재.**

## 줄기 3종 (owner 확정 명칭)

| 종류 | 정의 | 탄생 | 성장 | 소멸 |
| --- | --- | --- | --- | --- |
| **골격줄기** (skeleton) | SE/과제관리 지식에서 오는 뼈대 — 게이트 단계, SE 산출물 그룹, 계약·일정, 구매·납품, 품질 | 과제 등록 시 자동(core_deliverable·게이트 데이터에서 파생 — 추론 불요) | 불변(가지 추가는 owner) | 과제 종료 |
| **작업줄기** (work) | 시작과 끝이 있는 "한 건의 일" (예: 필터계수 전달) | **할일 정식 등록(confirm) 순간** (2026-07-05 owner 위임→추천 채택) | 같은 스레드 후속 메일 자동 귀속(E1 mail_followup 재사용) | 할일 완료 = 닫힘(열매) |
| **이력줄기** (history) | 끝나지 않고 시간축으로 쌓이는 흐름 — 회의체·정기보고 (owner 명명 2026-07-05) | 같은 정규화 제목 8주 내 3회 반복 감지 → **승격 제안 → 사람 1클릭 확정** (자동 승격 금지) | 회차(occurrence)가 가로로 추가 | 명시 종결만 |

핵심 관계: 이력줄기의 각 **회차에서 작업줄기가 분가**한다(회의 액션). 열린 작업줄기는
출생 회차 링크로 **다음 회차에 미결 이월**로 자동 표시된다(별도 저장 없이 파생 계산).

## 연결 등급 원칙 (owner 확정 2026-07-05)

> **단어는 후보 추천에만 쓰고, 연결의 확정은 ID·사람 확정·사용 사실로만 한다.**

- 1등급(확정 링크): 이력키(메일→할일 출생), Message-ID/스레드 키(후속 귀속), 사람이 찍은 SE anchor
  (할일→골격), 출생 회차 링크(할일→이력줄기 회차), 완료 상태.
- 2등급(추천만): 제목 키워드 매칭, LLM 가지 제안 — 확정으로 그리지 않고 제안 표시.
- **제목 조각이 가지가 되는 것 금지**: 소속 판정 전부 실패 시 가지를 만들지 않고 수신함 보류.

## 소속 판정 순서 (메일 1통 기준)

1. 기존 작업줄기 스레드 매칭(E1) → 귀속
2. 이력줄기 시리즈 제목 매칭 → 해당 회차로
3. SE 산출물/게이트 anchor 매칭 → 골격줄기
4. 규칙/키워드 → 후보 제안(확정 아님)
5. 전부 실패 → 보류(가지 미생성)

## 조작면 원칙 (드래그 = 사람 확정)

그래프에서 노드를 끌어 다른 곳에 붙이는 행위 = 링크 필드 1개 변경 = **사람 확정 이벤트**.
event_log 기록 + 장부 write-through + 엔진 오분류 교정 피드백으로 사용. 계약은 B6 문서 소유.

## owner 결정 기록 (2026-07-05)

1. 작업줄기 탄생 = 정식 등록(승인) 시점만 (owner: "너가 추천해서 새로 만들어도 되" — 추천 채택).
2. 이력줄기 명칭·개념 = owner 직접 제안("이력 줄기같은 느낌"). 승격 기준 8주 3회+1클릭은 위임 기본값(파라미터).
3. 조작형 그래프(드래그 재부착→데이터 반영) = owner 요구사항 확정.

## 비-목표

- LLM 이 가지를 자동 확정하는 것(추천까지만 — 코어 LLM 0% 유지).
- 기존 문자열 가지의 소급 재분류 자동화(legacy 표시 후 신규 생성만 v2 — 소급은 owner 판단).
- 음성/전화 채널 합류(전화 그릇 슬라이스에서 별도 — 합류 지점은 이 온톨로지를 따름).
