# P-13 — 작성법 위저드 — doc_recipe 화면, ARTIFACT_FLOW 7스텝 재사용으로 '하는 방법' 안내

- **phase**: UI
- **depends_on**: 없음
- **parallel_group**: appjs-recipe
- **owner_decision**: none (기본=회의록 1종, ARTIFACT_FLOW 7스텝 재사용, 읽기 전용 안내).

## allowed_write_paths
- /Volumes/OPENCLAW_WS/Soulforge/ui-workspace/apps/dev-erp/static/app.js
- /Volumes/OPENCLAW_WS/Soulforge/ui-workspace/apps/dev-erp/src/lexicon.mjs
- /Volumes/OPENCLAW_WS/Soulforge/ui-workspace/apps/dev-erp/src/guide.mjs
- /Volumes/OPENCLAW_WS/Soulforge/ui-workspace/apps/dev-erp/server.mjs
- /Volumes/OPENCLAW_WS/Soulforge/ui-workspace/apps/dev-erp/test/core.test.mjs

## summary
작성법 위저드: 보고서·회의록 같은 문서 종류의 '작성 방법(절차)'을 보며 작업하는 화면. 신규 백엔드 최소 — guide.mjs ARTIFACT_FLOW 7스텝(snapshot→sources→draft→review→action→final→quality)을 재사용하고, guide.mjs 에 DOC_RECIPES 상수 + docRecipes(mode) export. server.mjs GET /api/doc/recipes 라우트(읽기 전용·MVP=체크 미연결). 회의록 1종 시드.

## data_model
- 신규 테이블 0. guide.mjs DOC_RECIPES 상수: [{ key:'meeting_minutes', b:'회의록', f:'원탁 기록', required_input:[{b,f}...], steps: ARTIFACT_FLOW 기반 [{flow_key,name_b,name_f,tip_b,tip_f}] }] — 회의록 1종.

## code_changes
- src/guide.mjs: export const DOC_RECIPES = [{ key:'meeting_minutes', b:'회의록', f:'원탁 기록', required_input:[{b:'안건',f:'안건'},{b:'참석자',f:'참석자'},{b:'결정사항',f:'결의'}], steps: ARTIFACT_FLOW.map(s=>({ flow_key:s.key, name_b:s.b, name_f:s.f, tip_b:s.hint, tip_f:s.hint })) }].
- src/guide.mjs: export function docRecipes(mode='business'){ const k=mode==='fantasy'?'f':'b'; return DOC_RECIPES.map(r=>({ key:r.key, name:r[k], required_input:r.required_input.map(x=>x[k]), steps:r.steps.map(s=>({ flow_key:s.flow_key, name:k==='f'?s.name_f:s.name_b, tip:k==='f'?s.tip_f:s.tip_b })) })); }.
- server.mjs: import 에 docRecipes 추가(./src/guide.mjs); 라우트 'if (path === "/api/doc/recipes") return send(res, 200, docRecipes(qp.mode));' 를 /api/guide/templates(L148) 근처.
- static/app.js render(): 'if (state.view==="mod:recipe"){ $("#viewTitle").textContent=L.recipe_title; logView(state.view); return renderRecipe(); }'; renderGuide()/renderReports() 상단에 '작성법 위저드' 버튼(state.view='mod:recipe').
- static/app.js 신규 async renderRecipe(): const recipes=await api(`/api/doc/recipes?mode=${state.mode}`); state.recipeKey ??=recipes[0]?.key; 종류 select(change→state.recipeKey) + required_input 칩 + steps 체크리스트(7스텝 flow_key·name·tip), 읽기 전용 안내.
- src/lexicon.mjs business+fantasy 양쪽: recipe_title(작성법 위저드/제작 비법서), recipe_pick(문서 종류/기록 종류), recipe_required(필요 입력/필요 재료), recipe_steps(작성 절차/제작 단계), recipe_open(작성법 보기/비법 폴치기).

## ui_changes
- guide/reports 화면 상단 '작성법 위저드' 진입. renderRecipe(): 문서 종류 select + 필요 입력 칩 + 7스텝 절차(이름·팁), 읽기 전용. fantasy 비법서 용어 전환.

## test_cases
- TEST-003 lexicon parity: recipe_* 5키 양쪽 일치.
- 신규 node:test 'P-13: docRecipes 가 7스텝·required_input 반환·모드 전환': import { docRecipes } from '../src/guide.mjs'; const r=docRecipes('business'); assert.ok(r.length>=1&&r[0].steps.length===7&&r[0].required_input.length>=1); const f=docRecipes('fantasy'); assert.notEqual(f[0].name,r[0].name).
- 신규 node:test 'P-13: recipe flow_key 가 ARTIFACT_FLOW 키와 1:1': import { ARTIFACT_FLOW } from '../src/guide.mjs'; const keys=docRecipes('business')[0].steps.map(s=>s.flow_key).sort(); assert.deepEqual(keys, ARTIFACT_FLOW.map(s=>s.key).sort()).

## acceptance_checks
- node --test 전건 green.
- node server.mjs --db :memory: 기동 후 작성법 위저드→회의록 선택→필요 입력 3개·7스텝 절차(수동 verify), business/fantasy 토글 라벨 전환.
- curl -s 'http://127.0.0.1:4300/api/doc/recipes' 가 200 + recipes 배열.

## stop_conditions
체크 상태 영속화(guide_step 연결) 요구 시 MVP(읽기 안내) 벗어남 — 중단·후속. 회의록 외 종류(구매요청서 등) 추가 요구 시 required_input 어휘 결정 — 중단.

## guards
- ① zero-dependency(guide.mjs 상수만, 새 테이블·npm 0)
- ② 코어 LLM 0%
- ⑤ ALTER 불필요(스키마 무변경)
- ⑩ public-safe
- ⑪ lexicon parity
- ⑧ node:test 전건 green + 표기 commit+push
