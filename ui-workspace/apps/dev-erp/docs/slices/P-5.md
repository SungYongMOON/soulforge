# P-5 — 하드 게이트 확장 — artifact_requirement scope_kind='item_blocking' rule + gateEval reason 'blocking_items_open'(신규 게이트 함수 0)

- **phase**: GATE/스케줄
- **depends_on**: SE-DATA
- **parallel_group**: G-store
- **owner_decision**: 기본값+가정: mode='hard'(gateMode 기본), item_blocking artifact_type 와일드카드='any'. 세분 차단 사유별 규칙 defer.

## allowed_write_paths
- /Volumes/OPENCLAW_WS/Soulforge/ui-workspace/apps/dev-erp/src/store.mjs
- /Volumes/OPENCLAW_WS/Soulforge/ui-workspace/apps/dev-erp/src/lexicon.mjs
- /Volumes/OPENCLAW_WS/Soulforge/ui-workspace/apps/dev-erp/test/core.test.mjs

## summary
artifact_requirement 테이블을 재사용해 scope_kind='item_blocking' 행을 게이트 정책으로 해석한다: 단계(scope_key=stage_code)에 item_blocking 규칙이 있고 그 단계의 차단 할일(status='blocked')이 1개 이상이면 gateEval 이 reason {code:'blocking_items_open',n,detail} 를 추가하고 passable=false. 신규 게이트 함수 0 — gateEval 본문 인라인 분기만, 정책은 setArtifactRequirement(scope_kind='item_blocking') 로 등록. clearStage/gateMode 무변경.

## data_model
- DDL/ALTER 없음 — artifact_requirement 재사용. scope_kind 자유 TEXT 라 'item_blocking' 추가에 스키마 변경 0.
- 정책 행: artifact_requirement(scope_kind='item_blocking', scope_key=<stage_code>, artifact_type=<차단 분류 키, 기본 'any'>, label, mode='hard'|'soft', seq). UNIQUE(scope_kind,scope_key,artifact_type) 그대로.
- setArtifactRequirement scope_kind 기본='board_type' 이므로 호출 시 scope_kind:'item_blocking' 명시(이미 파라미터로 받음, store.mjs L1220). 함수 변경 0.

## code_changes
- store.mjs gateEval(stage): blocked 변수(L774 에서 이미 계산됨) 재사용. reason 수집부(required_artifacts_missing 블록 L800 다음)에 인라인 추가: const blockingRule = this.db.prepare("SELECT mode FROM artifact_requirement WHERE scope_kind='item_blocking' AND scope_key=? LIMIT 1").get(stageCode); if (blockingRule && blocked > 0) reasons.push({ code:'blocking_items_open', n:blocked, detail:{ stage_code:stageCode, mode:blockingRule.mode } });
- store.mjs gateEval: passable(L802 reasons.length===0)·remaining(L804 blocked 포함) 추가 로직 0 — 새 reason 이 자동으로 passable=false. clearStage(L821-829)·gateMode 무변경.
- lexicon.mjs business 블록 gate_reason_required_artifacts_missing(L134) 다음에 gate_reason_blocking_items_open: '하드 차단 사유 미해결'; fantasy 블록 L407 다음에 gate_reason_blocking_items_open: '봉인 해제 실패'. (renderGates 의 L[`gate_reason_${r.code}`] 가 양 모드 라벨 찾도록.)

## ui_changes
- UI 와이어링 없음(이 패킷). renderGates 의 reason 표시는 lexicon 매핑으로 새 code 자동 렌더 — app.js 변경 0. UI 검증은 SE-UI.

## test_cases
- 'P-5: item_blocking 규칙+차단 할일 1개 → reason blocking_items_open + passable=false': freshStore(); loadFixture(store); store.upsertStage({id:'st-p5',project_id:'PRJ-A',title:'상세설계',stage_code:'120',seq:1}); const it=store.createItem({project_id:'PRJ-A',title:'블록작업'}); store.db.prepare("UPDATE core_item SET stage_id='st-p5', status='blocked' WHERE id=?").run(it.item.id); store.setArtifactRequirement({scope_kind:'item_blocking',scope_key:'120',artifact_type:'any',label:'차단',mode:'hard'}); const stage=store.gates({project:'PRJ-A'}).find(s=>s.id==='st-p5'); assert.equal(stage.reasons.find(x=>x.code==='blocking_items_open')?.n,1); assert.equal(stage.passable,false).
- 'P-5: 규칙 없으면 reason 미발생(하위호환)': 위와 동일하되 setArtifactRequirement(item_blocking) 생략. 기대: stage.reasons 에 blocking_items_open 없음(기존 blocked_items reason 은 존재).
- 'P-5: hard 모드 item_blocking 미해결 → clearStage gate_blocked': 위 차단+규칙 후 store.setGateMode('hard'); const r=store.clearStage('st-p5'); assert.equal(r.error,'gate_blocked'); assert.ok(r.reasons.find(x=>x.code==='blocking_items_open')); 그 후 setItemStatus(it.item.id,'done') 후 clearStage('st-p5').ok===true.
- 'P-5: lexicon 양 모드 라벨 존재': import {getLexicon}; assert.ok(getLexicon('business').gate_reason_blocking_items_open && getLexicon('fantasy').gate_reason_blocking_items_open).

## acceptance_checks
- node --test 전건 green.
- gateEval 시그니처/반환 키 불변(기존 게이트 테스트 통과).
- 신규 게이트 함수 0 — diff 에 새 메서드 정의 없음, gateEval 본문 인라인 reason 분기만.
- item_blocking 규칙 미정의 과제는 P-5 이전과 동작 동일(회귀 0).

## stop_conditions
item_blocking artifact_type 의미가 owner 정책으로 'bottleneck_reason 별 세분 차단'을 요구하면 detail 에 GROUP BY 필요해 범위 확대 — 기본값 'any'(단계 모든 blocked 차단)로 진행, 세분 차단 명시 요구 시 멈추고 보고.

## guards
- ⑥ gateEval/clearStage/gateMode 신규 함수 0(reason 인라인만)
- ⑤ ALTER 0(artifact_requirement 재사용), setArtifactRequirement ON CONFLICT 멱등
- ② 코어 LLM 0%(규칙은 사람 등록)
- ④ event_log append-only(gate_clear 기존 server 경로)
- ① zero-dependency(새 import 0)
- ⑧ node:test 전건 green + 표기 commit+push
