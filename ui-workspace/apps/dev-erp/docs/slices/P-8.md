# P-8 — app.js: 일정 뷰어 카드 — 마감 3버킷(연체/이번주/이후) deadline_cal 위젯 ready 화 + 전용 본문

- **phase**: UI
- **depends_on**: 없음
- **parallel_group**: appjs-home-widgets
- **owner_decision**: none (기본=마감 있는 할일만, 휴가/프로젝트 일정 due 없으면 제외).

## allowed_write_paths
- ui-workspace/apps/dev-erp/static/app.js
- ui-workspace/apps/dev-erp/src/lexicon.mjs

## summary
홈 위젯 deadline_cal(WIDGET_PLAN L286, 현재 ready 없음)을 ready:true 로 켜고 widgetBody('deadline_cal') 분기 추가. /api/items 의 due 만으로 클라이언트에서 3버킷: 연체(due<today&status!=='done'), 이번주(today<=due<=today+6), 이후(due>+6). 각 버킷 상위 N건 미니표. WIDGET_CATALOG 는 ready 자동 포함(L310). tile_deadline_cal 라벨 이미 존재.

## data_model
- (없음)

## code_changes
- static/app.js WIDGET_PLAN(L286): { id:'deadline_cal', cat:'group_task' } → { id:'deadline_cal', cat:'group_task', ready:true }.
- static/app.js widgetBody(id) 분기: if (id === 'deadline_cal') { const items=await api('/api/items'); const t=new Date().toISOString().slice(0,10); const wk=new Date(Date.now()+6*86400000).toISOString().slice(0,10); const over=items.filter(i=>i.due&&i.due<t&&i.status!=='done'); const soon=items.filter(i=>i.due&&i.due>=t&&i.due<=wk&&i.status!=='done'); const later=items.filter(i=>i.due&&i.due>wk&&i.status!=='done'); const sect=(lab,arr,cls)=>arr.length?`<tr class='date-sep'><td colspan='3' class='${cls}'>${lab} ${arr.length}</td></tr>`+arr.slice(0,5).map(i=>miniRow([esc(i.title),esc(i.project_id),i.due])).join(''):''; return { title:L.tile_deadline_cal, html:(over.length||soon.length||later.length)?`<table><tbody>${sect(L.bucket_overdue,over,'due-over')}${sect(L.bucket_thisweek,soon,'')}${sect(L.bucket_later,later,'')}</tbody></table>`:`<div class='empty'>${L.empty_items}</div>` }; }
- src/lexicon.mjs business+fantasy 양쪽: bucket_overdue(연체/기한 초과), bucket_thisweek(이번 주 마감/금주 관문), bucket_later(이후 예정/다가올 운명).

## ui_changes
- 홈 위젯 서랍 group_task 에서 deadline_cal 드래그 가능(ready). 위젯 본문 연체(빨강)/이번주/이후 3섹션 미니표, 각 5건. 팝아웃·새로고침·접기 기존 인프라 동작.

## test_cases
- TEST-003 lexicon parity: bucket_* 3키 양쪽 일치.
- 신규 node:test 'P-8: items due 노출 + 마감 항목 증가': freshStore()+loadFixture(store); assert.ok(store.items({}).some(i=>'due' in i)); const before=store.items({}).filter(i=>i.due).length; store.applyTemplate('PRJ-A','120_CDR',{anchorDates:{'120':'2026-08-01'}}); const after=store.items({}).filter(i=>i.due).length; assert.ok(after>before).

## acceptance_checks
- node --test 전건 green.
- node server.mjs --db :memory: 기동 후 홈에서 ❙❙ 서랍→deadline_cal 드래그→연체/이번주/이후 3섹션(수동 verify).
- 위젯 팝아웃(⤢)·새로고침(⟳) 정상.

## stop_conditions
WIDGET_CATALOG 가 ready 위젯 자동 포함 안 하는 회귀가 있으면(현재 L310 filter ready) 중단. 간트/캘린더 격자 요구 시 MVP(3버킷 카드) 벗어남 — 중단·후속.

## guards
- ① zero-dependency
- ② 코어 LLM 0%
- ⑩ public-safe
- 읽기 전용(쓰기 0)
- ⑪ lexicon parity
- ⑧ node:test 전건 green + 표기 commit+push
