# P-12 — app.js: 자동보고 UI 와이어링 — reports_w 위젯 ready 화 + 보고서 화면 점프(자동발신 0)

- **phase**: UI
- **depends_on**: 없음
- **parallel_group**: appjs-home-widgets
- **owner_decision**: none (기본=미리보기·복사만, 자동발신 0).

## allowed_write_paths
- /Volumes/OPENCLAW_WS/Soulforge/ui-workspace/apps/dev-erp/static/app.js
- /Volumes/OPENCLAW_WS/Soulforge/ui-workspace/apps/dev-erp/src/lexicon.mjs

## summary
자동보고 생성기는 이미 renderReports()(mod:reports)에 있고 /api/worklog/draft·/api/report/draft 라우트도 존재. P-12 는 홈 위젯 '노출' 와이어링: reports_w(WIDGET_PLAN L288, 현 ready 없음)을 ready 화하고 widgetBody('reports_w') 에 최근 7일 worklogDraft 첫 줄 미리보기 + '보고서 화면으로' 버튼. 위젯/팝아웃에서 mod:reports 점프. 자동발신 금지 — 미리보기·복사만. P-8 과 같은 widgetBody 군이라 동일 parallel_group(순차).

## data_model
- (없음)

## code_changes
- static/app.js WIDGET_PLAN(L288): { id:'reports_w', cat:'group_doc' } → { id:'reports_w', cat:'group_doc', ready:true }.
- static/app.js widgetBody(id): if (id === 'reports_w') { const d=await api('/api/worklog/draft?days=7'); const preview=(d.text||'').split('\n').slice(0,6).join('\n'); return { title:L.tile_reports_w, html:`<pre class='gen-preview mini'>${esc(preview)||L.gen_run}</pre><button class='fav-chip mini' data-goreports='1'>${L.reports_open}</button>` }; }
- static/app.js bindWidgetInner()(L1112): '$("#view").querySelectorAll("[data-goreports]").forEach(b=>b.addEventListener("click",(e)=>{e.stopPropagation(); state.view="mod:reports"; render();}));'.
- static/app.js openPopout()(L1093) 내부에도 동일 data-goreports 바인딩(팝아웃 점프).
- src/lexicon.mjs business+fantasy 양쪽: reports_open(보고서 생성기 열기/연대기 작성소 열기) — tile_reports_w·gen_* 기존 키 재사용.

## ui_changes
- 홈 서랍 group_doc 에서 reports_w 드래그 가능(ready). 위젯 최근 7일 업무일지 초안 미리보기(6줄)+'보고서 생성기 열기'. 버튼/팝아웃→mod:reports 점프.

## test_cases
- TEST-003 lexicon parity: reports_open 양쪽 일치.
- 신규 node:test 'P-12: worklogDraft/reportDraft 가 위젯 미리보기 텍스트 제공': freshStore()+loadFixture(store); const d=store.worklogDraft({days:7}); assert.ok(typeof d.text==='string'&&d.text.length>0); assert.ok(store.reportDraft({kind:'report'}).text.length>0).

## acceptance_checks
- node --test 전건 green.
- node server.mjs --db :memory: 기동 후 홈에서 reports_w 추가→업무일지 미리보기→'보고서 생성기 열기'→mod:reports 이동(수동 verify).
- mod:reports 에서 worklog/report/note 생성+복사 정상(회귀 없음).

## stop_conditions
자동발신/메일회신 자동화 요구가 들어오면 즉시 중단(발신 경계 — 절대 금지 defer). worklogDraft 응답이 text 외 구조 의존 필요 시 서버 라우트 확장 별 패킷.

## guards
- ② 코어 LLM 0%(템플릿 결정적 초안, LLM 다듬기 미연결)
- ③ 원문 미저장(메타/포인터 기반 초안)
- 발신 0(미리보기·복사만)
- ① zero-dependency
- ⑩ public-safe
- ⑪ lexicon parity
- ⑧ node:test 전건 green + 표기 commit+push
