// The HPP smoke runner uses app-relative entries. Reuse the owned recovery gate
// here so source and installed copies exercise the same protected generation.
// Explicit from syntax also exposes the dependency to the shared Pack walker.
import {} from '../../../../guild_hall/dev_worker/feedback_restore.test.mjs';
