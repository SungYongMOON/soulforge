# manifests

- 무엇을 둔다: `example_class` 의 capability package manifest, dependency, validation reference 설명.
- 무엇을 두지 않는다: project run log, workflow history, operational metrics.
- 왜 이렇게 둔다: manifest 는 reusable package metadata 이고, 실행 truth 는 다른 owner 가 가져야 한다.
