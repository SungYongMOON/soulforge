{
  "schema": "soulforge.source_lane_spec.v0",
  "lane_id": "workspace-ledgers-v4",
  "description": "v4 (2026-09-22): 메일함 (027_수신이력_이동이력/메일_수신이력.csv, 메일_발송이력.csv) now renders the mail's real mailbox owner(s) -- '<display_name> <email>' off each custody line's own metadata.mailbox block (team_mailboxes.py's TeamMailbox.metadata() shape, confirmed against a real hiworks custody event) -- instead of the fixed per-source collector label it always rendered before. A mail deduplicated across more than one team member's own mailbox lists every owner it was found in, joined by ' ; ', in the stable order mail_events.mjs's new ownersOf produced; a mail with no metadata.mailbox block on any custody line falls back to the unchanged fixed label, counted in refresh()'s new receipt field mailbox_owner_fallback. No column was added, renamed or removed (the CSV header is pinned and unchanged), no row's identity/key changed (이력키 never depended on this and still does not), and no Owner-entered column's preservation-by-key contract changed. Four files changed, all inside this module: src/mail_events.mjs (formatMailboxOwner, ownersOf, candidate.mailbox plumbing through collectCandidatesFromDirs/dedupeAndAssignIds), src/common_events.mjs (loadRawMailRecords now carries mailbox_owners through), src/ledgers.mjs (mailboxCellOf, used by buildHistoryRow in place of the old bare mail.source), src/refresh.mjs (the new mailbox_owner_fallback receipt counter). ops/mail_attribution_index.mjs is untouched -- its per-mail record has no field for a mailbox owner and its own header note is explicit that it never publishes a name; adding one there would violate that module's own privacy boundary. Bumped to v4 (not patched in place) for the same reason every prior bump was: lanes are never rebuilt in place, so a fresh directory is how a drift check can tell an old, running lane from this fix. Every v1/v2/v3 fact below (tracked_paths, entry_points, import closure) is unchanged: no file moved, no new import was added, nothing here needs node_modules or any other untracked closure, and this bump registers no task, installs no skill into any bot profile and builds no lane by itself.\n\nv3 (2026-09-22): a bot-triage follow-up fix, no import-closure change. Three cosmetic defects found on the first live trial: (1) ops/bot_triage.mjs wrote a bot-appended row's 수신일 as the raw ISO instant instead of the Seoul calendar date every other row in the Owner's table already uses -- now converted with src/ledgers.mjs's seoulDateOf, and a missing/unparseable received_at becomes '' rather than throwing or writing garbage/Invalid-Date text; src/triage.mjs's appendReadingDecision gained the same normalisation for its own receivedAt param (used by cli.mjs triage decide --received-at too), with an already-YYYY-MM-DD value passing through unchanged and an unparseable non-empty value kept as typed (not newly refused). (2) appendReadingDecision stamped 판독일 as `now.slice(0, 10)` (a raw UTC date), which reads as yesterday's date for a decision made before 09:00 UTC (before local midnight in Seoul) -- now `seoulDateOf(now)`, matching every other display date in this module. Neither column is read by any classification path in this module (informational only), so this bump changes no attribution behaviour. (3) ops/bot-skill/SKILL.md's answer-format section now says explicitly to end an answer with the tool's own `오늘 판독 n/N건` line and never append a call-count/investigation-budget footer from another skill in the same bot profile; SKILL.md's own `version` front-matter field moved 0.1.0 -> 0.1.1 (that field is prose the installer copies verbatim -- it does not change install_skill.mjs --check semantics, which compares rendered bytes as a whole either way). Bumped to v3 (not patched in place) because lanes are never rebuilt in place -- a fresh directory is how a drift check can tell an old, running lane from this fix. Every other v1/v2 fact below (tracked_paths, entry_points, import closure) is unchanged: no file moved, no new import was added, nothing here needs node_modules or any other untracked closure, and this bump registers no task, installs no skill into any bot profile and builds no lane by itself. v2 (2026-09-22): the lane now also carries the bot-facing 판독 wrapper (ops/bot_triage.mjs -- a fixed, config-pinned list/show/decide surface a local chat bot is given INSTEAD of cli.mjs's free-argument `triage` subcommands, so the reader identity, the tables and the daily budget are pinned by this lane rather than asserted by the model) and its skill template plus installer (ops/bot-skill/SKILL.md, ops/bot-skill/install_skill.mjs). Import closure walked again from the two new .mjs entry points: ops/bot_triage.mjs imports only ../src/{ledgers,owner_tables,refresh,rule_store,triage}.mjs -- every one already inside this module -- plus node: builtins, and ops/bot-skill/install_skill.mjs imports node: builtins only, so tracked_paths needed no new entries (the whole-module prefix below already carries both new files and the skill folder). entry_points gains the three new files so a built lane's Resolve-CanonicalFile can find them; every v1 entry point below is kept unchanged. This bump registers no task, installs no skill into any bot profile and builds no lane by itself -- rendering the skill into a profile directory and enabling it there is a separate Owner action. v1 (2026-09-22): the daily unattended runner for guild_hall/workspace_ledgers (ops/daily_refresh.mjs, calling refresh() then refreshCommon() from one org config -- see the module README's \"매일 갱신 lane\" section) plus its scheduled-task registrar and hidden launcher (ops/register-workspace-ledgers-task.ps1, ops/run-workspace-ledgers-hidden.vbs). Import closure walked from ops/daily_refresh.mjs and cli.mjs: every relative import in guild_hall/workspace_ledgers/src/*.mjs and guild_hall/workspace_ledgers/ops/daily_refresh.mjs resolves to another file inside guild_hall/workspace_ledgers/ itself (confirmed by grep over every `from './...'`/`from '../...'` import line in src/, ops/ and cli.mjs -- none reaches outside the module; the only imports outside that are node: builtins). tracked_paths is therefore the whole module directory, wholesale, the same shape context-read-v5/graph-sync-v1 use for guild_hall/context_engine/ -- there is no carried_forward_prefixes entry because nothing here needs node_modules or any other untracked closure. This spec bump registers no task and builds no lane by itself.",
  "tracked_paths": [
    "guild_hall/workspace_ledgers/"
  ],
  "tracked_excludes": [
    "guild_hall/workspace_ledgers/tests/"
  ],
  "carried_forward_prefixes": [],
  "entry_points": [
    "guild_hall/workspace_ledgers/cli.mjs",
    "guild_hall/workspace_ledgers/ops/daily_refresh.mjs",
    "guild_hall/workspace_ledgers/ops/register-workspace-ledgers-task.ps1",
    "guild_hall/workspace_ledgers/ops/run-workspace-ledgers-hidden.vbs",
    "guild_hall/workspace_ledgers/ops/bot_triage.mjs",
    "guild_hall/workspace_ledgers/ops/bot-skill/SKILL.md",
    "guild_hall/workspace_ledgers/ops/bot-skill/install_skill.mjs"
  ]
}
