# Integration door

This worktree owns only this domain package. The factory integration lane may,
after its own sequential validation and review, add the descriptor to shared
engine topology/manifest/release surfaces and extend global conformance tests.

This package requests no Core API change and no root package, registry-schema,
Watchtower, purchasing, ERP, task, or production binding change. The exact
proposal and conditions are in [integration_request.md](../integration_request.md).
