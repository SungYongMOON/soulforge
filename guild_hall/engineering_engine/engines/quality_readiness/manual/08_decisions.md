# 08. Decisions

Common chassis: [../08_decisions.md](../08_decisions.md).

The accepted source packet is an implementation-source binding, not a production/canon adoption. Rule-stage-Owner bindings are caller-supplied exact bindings; E01 does not mint, infer, or authorize them. The common manifest's bare `claim_ceiling: source_supported` remains documented compatibility debt; E01 outputs name canon and evidence axes explicitly. Owner decisions outside that binding remain HOLD.
