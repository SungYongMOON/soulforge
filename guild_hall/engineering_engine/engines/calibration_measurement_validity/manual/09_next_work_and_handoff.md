# 09 — Next work and handoff

The factory integration lane owns the package registration, root focused validator, shared topology/manifest/release regeneration, and all global conformance reruns. See [integration request](../integration_request.md).

Possible later work requires an explicit owner/source package:

- approved profile semantics for interval or exception policy;
- unit-conversion authority and a conversion source;
- approved uncertainty-model and guard-band semantics;
- a project-local adapter that creates typed facts from properly authorized evidence; and
- a private zero-write pilot.

None of those is implied by this candidate package.
