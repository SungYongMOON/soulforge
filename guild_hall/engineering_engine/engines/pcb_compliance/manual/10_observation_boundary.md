# 10. Observation boundary

The evaluator consumes supplied typed observations only. It neither reads a filesystem nor scans
project material. Evidence status means the bound observation says `present`, `absent_confirmed`,
`conflict`, or `unknown`; it does not prove a process was performed correctly.
