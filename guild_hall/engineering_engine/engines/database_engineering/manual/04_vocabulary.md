# 04 — Vocabulary

The package maps only to the existing Core states: `satisfied`, `gap_missing`,
`gap_unknown`, and `gap_conflict`. There is no risk state or single readiness
score. `gap_missing` requires project requirement, exact platform, and
contradictory machine-observable evidence.
