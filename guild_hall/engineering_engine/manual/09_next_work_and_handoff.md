# 09. 다음 작업과 인수인계

## 9.0 확정 계획 (Owner 확정 2026-08-18 밤 — 이 순서대로 간다. 바꾸면 여기 먼저 고친다)

### 9.0.1 목표와 "쓴다"의 정의

목표: 엔진이 "없다"만이 아니라 **"이제 이걸 이렇게 하라"**를 체계공학 근거(정본 인용)와 함께 답하고, 그 답을 서브 에이전트와 팀원이 MCP로 받아 움직이게 한다.

| 이정표 | 뜻 | 조각 |
| --- | --- | --- |
| M1 | Owner가 "이제 뭐 해야 해?"라고 물으면 위치·부족·다음 3개(왜·어떻게·근거·담당)가 나온다(스크립트) | A1~A3 |
| M2 | 팀원·AI 비서가 MCP로 묻고 답하고, 밤에 자동으로 돈다 | B1~B3 |
| M3 | 새 과제도 하루 안에 같은 답 | C1~C2 |
| M4 | "제대로 됐냐"·"잘 하고 있냐"·"어떻게 판단할까"까지 | D1~E1 |

### 9.0.2 원칙 (변하지 않는 것)

1. 한 번에 한 조각. 조각은 시험·영수증·문서 동기화·commit+push로 닫는다.
2. 판단(A1·A2·D1·D2)은 결정론이고 문서를 직접 읽지 않는다. 안내(A3·E1)는 정본 인용 붙인 안내이며 판단을 바꾸지 않는다.
3. 엔진은 맥락과 벽으로 막혀 있다: 들어오는 것은 **종이 3종**(있음/성숙도 · 내용 상태 · 요구 커버리지)뿐, 나오는 것은 판단 영수증과 mission 후보(→ 지시서)뿐. 맥락 → 엔진은 후보 → 사람 확인 → 관측(규칙이 명확한 03_Out만 자동 확정). 엔진 → 맥락은 별도 writer·별도 권한.
4. 규칙·관계·인풋아웃풋은 정본이 말한 것만 인용 위치와 함께. 관행은 `unstated`로 약하게. 우선순위 규정 > 가이드북 > 일반 SE 지침 > 미표기. 덧씌움은 더할 수 있고 정본 것을 지우지 못한다(D45).
5. 새 기초(잠금·계약·store·top-level root)는 더 만들지 않는다. 밖에서 부르는 문은 MCP 하나(9.1A).

### 9.0.3 조각 (순서 = 실행 순서)

| # | 조각 | 정본·입력 | 만드는 것 | 완료 기준 |
| --- | --- | --- | --- | --- |
| **A1** | 관측 확대(있냐 + 성숙도) | 과제 자료: TB_Master_Doc·메일 첨부(INBOX)·단계 폴더 03_Out; 어휘·스펙 행 | 관측 공급자: 문서→산출물 종류 분류 **후보**(제목·양식·본문 단서) + 성숙도(D/U/F: 파일명 접미사·판번호·승인 기록) → Owner 확인 → `artifact_observations`. 03_Out은 자동 확정 | KVDS 불명 95가 있음/없음/불명으로 다시 갈리고 run 05 재현(바이트 동일). 자동 분류는 후보로만(D37 원칙) |
| **A2a** | 활동·산출물 관계 사실 추출 | ① NASA NPR 7123.1D **부록 C**(공통 기술 프로세스 17개 입력·출력; 부록 F·G만 뽑혀 있어 C 추가 추출), NASA SE Handbook 4~6장 프로세스 입출력, DoD SEG §4 · ② 방사청 SE 기반 기술검토회의 가이드북 2017/2024 회의별 **INPUT/OUTPUT 표**, SE 기술관리 실무지침서 활동 절차 · ③ 발주처 품질지침 Q 게이트 입력 · ④ SOW/CDRL | 리더 병렬 → 활동(요구분석·기능분석/할당·아키텍처·예비/상세설계·통합·검증·확인·형상관리·위험관리·트레이드·PBS 확정·리서치 등)별 `inputs[]/outputs[]`와 인용, 층별 파일 | 정본별 추출 JSON + 스팟체크(인용 재독) 영수증 |
| **A2b** | 규칙표 확장(D46) | A2a 결과, 스펙 4종·어휘 | 스펙 행에 `node_kind: artifact|activity|decision`, `depends_on[]`(입력, 각 간선에 source_ref·등급), 활동·결정 토큰(계열 activity/decision) 추가, exporter pass-through, `--check` | 스펙↔compiled 일치, 어휘 밖 토큰 0, 간선마다 인용 또는 unstated 표기 |
| **A2c** | 순서 계산(컴파일러) | A2b compiled | 컴파일러에 정렬 함수: (1) 입력이 갖춰지지 않은 것부터 (2) 규정 필수 (3) 이번 게이트 진입 조건; 인과(입력 간선)와 순서(게이트)를 분리해 둘 다 출력; 덧씌움은 간선 추가만 | 시험: 빈 과제(관측 0)에서 사업유형·등급·발주처별 "무엇부터" 목록이 결정론으로 나옴; ②+③+④ 계층 = 통합 등가 유지 |
| **A3** | 가이드 카드 + 지시서(D47) | 스펙 행(근거·양식·성숙도·입력) + 정본 색인(12건+) + 어휘 담당 capability + mission 후보 | 산출물·활동별 카드(왜·언제·무엇을·어떻게·근거 인용·양식·앞뒤 관계·담당) 생성기 + `instruction_packet_v0`(무엇을·왜·입력·산출·근거·담당·기한·판단 근거) — zero-write, 판단 불변 | **착지 2026-08-18**: `guidance/` 3모듈 + CLI 1개, 시험 42(`validate:se-guidance`). KVDS 030_SRR·120_CDR에서 답 1회 — 카드 22·28, 지시서 3·3, 판정 수치는 run 04 그대로(이 층이 바꾼 것 0). **M1 도달**. 남은 것: 재컴파일한 정책으로 판정 재실행(요구 22·28 ↔ 판정이 센 19·27), 맥락 채움(기한·담당) 공급 경로 |
| **B1** | 엔진 MCP 서버(feature-OFF 제작) | A 결과, runner, 잠금 규칙 | `guild_hall/engineering_engine/mcp/` 도구 6: 규칙 보기·판단·다음 할 일·가이드 카드·관측 넣기·답 넣기. 로직 0, launch+sha·grant·zero-write·영수증 동일 | 시험 통과, 켜기는 Owner 결정 |
| **B2** | 답변 우편함 | kernel Phase 3 영수증 검증(있음), MCP | 사람 답(있음/해당 없음/근거·누가·언제) → 답변 영수증 + 관측/결정 사실 → 다음 실행 관측. P5 승인자 등록은 Owner | 같은 질문 반복 0, 영수증 쌍 검증 통과 |
| **B3** | 야간 예약 실행 | 운영 체크아웃 | 주 1회 runner + 브리프 | 사람이 안 눌러도 돎. **M2** |
| **C1** | 과제 착수 명령 | 폴더트리 스킬, 컴파일러, base packet 틀 | 5입력(코드·사업유형·발주처·등급·시작일) → 폴더트리·전 단계 컴파일·빈 과제 덧씌움·결속 뼈대·base packet 틀·첫 판단 | 새 과제 하루 안에 |
| **C2** | 다른 발주처·사업유형 | 발주처 품질지침(한화 등), 대조표 §10, 정본 881·974호 | 발주처 overlay 추가; 탐색·선행·운용 스펙 재기준 → 실제 과제 1건 검증 후 승격(D43) | **M3** |
| **D1** | 문서 내용 검사기 | 스펙 `양식`, 산출물별 필수 절·표(정본·발주처 지침) | 문서 읽어 `내용 충족/부족/미검사` 관측(종이 2) — 미검사는 부족으로 찍지 않음 | "있는데 부실"까지 답함 |
| **D2** | 요구 추적 후반 | R1/R2 준비 조각 | R2 원장 writer(Owner 승인 소량 pin) → R3 투영·generation → R4 카드 | 요구 118 추적(종이 3). **M4 전반** |
| **E1** | 조언 lane | se_core 출처 붙인 답변 lane(평가용) | 정본 인용 붙인 조언, 판단과 분리 표시 | **M4** |

### 9.0.4 맥락(메일·Slack·음성·파일)이 쓰이는 다섯 자리

| 쓰임 | 엔진 접점 | 조각 |
| --- | --- | --- |
| 관측 후보(어디에 무슨 문서가, 성숙도) | 종이 1 | A1 |
| 발주처 요청·"해당 없음" 결정 | 과제 덧씌움·답변 | A1(덧씌움 후보)·B2 |
| 사건·기한(회의 날짜·액션아이템) | 지시서의 기한·위험 | A3·B2 |
| 사람·담당(누가 냈나) | 지시서의 담당 라우팅 | A3 |
| 사연(왜 이렇게 됐나) | 가이드 카드·조언 인용, 감사 추적 | A3·E1 |

맥락 층 본체(원장·투영·기억 방식)는 이 계획 밖의 별도 트랙이며, 이 계획은 원장 없이도 굴러가게 만든다(오늘 KVDS도 그렇게 돌렸다).

### 9.0.5 Owner 결정 (계획 진행 중 필요)

D42~D45(추천대로면 됨) · D46(활동·결정 노드) · D47(지시서 계약) · MCP 켜기 시점 · Needs 정책 후보 · D40(중복 4쌍) · P5 승인자 등록.

### 9.0.6 이번 세션(2026-08-18) 이후 첫 걸음

다음 세션은 **A1**부터. A1이 끝나면 A2a(정본 추출)로 넘어가며 A2a는 병렬 리더로 하루 안에 가능하다.

## 9.1 다음 조각 (상세 참고표)

| # | 조각 | 입력 | 출력 | 완료 기준 |
| --- | --- | --- | --- | --- |
| 1 | **①↔② 토큰 별칭 정합** | ① `generic_se_base.json`, ② `system_dev_common_no_grade.json`, 어휘 | 어휘에 canonical+aliases(또는 ② 토큰 정규화 + 반복 행), 비교 드라이버 재실행 | 같은 게이트에서 ①∩② 토큰 수가 사람이 검토한 대응표와 일치, 시험 추가, D44 표시명 표와 함께 Owner 확인 |
| 2 | **표준 항목 관측 확대 + runner 재실행(27 정책)** | run 03 packet, 120_CDR 정책 27, 과제 폴더·색인 관측 | run 04(zero-write) | 불명 16~18 중 관측 가능한 것이 present/absence_confirmed로 바뀌고 mission 후보가 담당 capability로 나옴 |
| 3 | **NASA SE Handbook 반영(① 보강)** | `src_NASA_SE_HANDBOOK_*.json`, ① 스펙 | 행 인용 보강 또는 행 추가, 도출 기록 §6-1 닫기 | 3-출처 바닥 규칙 재확인, `--check`·시험 통과 |
| 4 | **R2 원장 writer** | R1/R2 준비 조각, Owner 승인 소량 pin(rtm 25 + contract_sow 18, 한 stage) | JSONL 원장 4종 writer + 재생 parity | 설계 §8 R2 완료 기준 4개, `guard:workmeta-write` 전건 |
| 5 | **커버리지 generation 생성기(R3 후반)** | R2 원장 | `projections/rtm/generations/<id>/` + packet | 같은 cutoff 두 번 → digest 동일 |
| 6 | **탐색·선행·운용·응용 variant 재기준(D43)** | 대조표 §10 초안, 정본 881호·974호 | draft variant 스펙 | 실제 과제 1건 검증 전 승격 금지 |
| 7 | **문서 내용 검사기(설계 §2.1A)** | 산출물 원문(private) | 산출물 안 필수 절·표 존재 판정 | zero-write, 정본 기준 |
| 8 | R4 카드·ERP read model·MCP 뷰 | R2/R3 | 얇은 카드, SQLite read model, MCP 뷰 4종 | 7조건 게이트 리포트 |
| 9 | **답변 우편함(Context Response 수신 경로)** — MCP·P5 시점 | 독립검토 스레드(V1.2, 2026-08-11) 지적: 엔진은 질문+답변 영수증 둘 다 있어야 판정을 바꾸는데 답변을 실제로 받는 경로가 없음. `kernel/context_receipt.mjs`(Phase 3, 합성 전용·전송 없음)에 영수증 **검증**만 있고 runner는 질문을 내지도 답을 읽지도 않음 | 우편함 writer(답 → 답변 영수증+후보, private) + runner의 질문 발행/답변 읽기 + P5 승인자 등록 | Owner 판단(2026-08-18): **MCP 구현 단계에서** 함. 그때까지 답은 `artifact_observations`(누가·언제·근거)로 수동 공급하고 엔진 코드는 바꾸지 않는다 |

정리 잔여(엔진 밖): ai_usage_meter 상태 폴더 해시 64→16(수집기 정지 창), `_workspaces/system` 실험·git_push 잔재, P23-037/RAW 뿌리 축약, 지식 intake 파일 61개 슬러그 반복, 휴지통 삭제(09-17 이후).

## 9.1A 출시 형태 (Owner 결정 2026-08-18)

- **밖에서 엔진을 부르는 정문은 MCP 하나다.** 팀원·AI 비서·Owner 모두 MCP로 묻고(판단·결손·불명), 답·관측도 MCP로 넣는다. 터미널 CLI 제품은 만들지 않는다(다른 회사 개발자에게 공급할 때만 껍질로 추가 검토).
- 지금 있는 스크립트(runner·드라이버)는 내부용이다: 개발·검증·야간 예약 실행이 치는 것이며 사용자에게 배포하지 않는다. "CLI 배포"라는 별도 산출물은 없다 — 배포되는 것은 엔진 코드 한 벌(운영 체크아웃 최신화)뿐이다.
- MCP 서버는 dev-ERP 안이 아니라 엔진 owner 아래 독립으로 둔다(예: `guild_hall/engineering_engine/mcp/`). dev-ERP는 존폐 미정이므로 엔진 출시 경로가 그것에 의존하지 않게 한다.
- MCP 도구는 로직을 갖지 않고 기존 순수 함수·runner를 그대로 부른다(한 로직, 여러 호출자). 잠금은 하나: launch 파일 + sha 핀 + Owner 동결 grant + zero-write + `_workmeta` 영수증. MCP 활성화 시점·답변 우편함(9.1의 9번)은 Owner 결정.
- 밤에는 예약 실행이 같은 runner를 부른다(엔진용 예약은 아직 미설정).

## 9.2 새 작업자(LLM 포함) 시작 체크리스트

1. `AGENTS.md` → `docs/architecture/foundation/AGENT_EXECUTION_CONTRACT_V0.md` → 이 매뉴얼 README·01·02·03 순으로 읽는다. 관련 없는 문서를 선적재하지 않는다.
2. HEAD·`.git/index.lock`·dirty change ownership 확인. 다른 스레드(예: Codex 엔진 스레드)가 실행 중이면 repo 무접촉.
3. 바꾸려는 것이 규칙이면 **스펙 md**를 고친다(코드 아님). exporter → `--check` → `validate:se-stage-rules` → `validate:se-foldertree-compiled` → 실제 과제 1건 재컴파일.
4. 새 파일·폴더는 `npm run validate:path-length`(예산 200/60/60/해시 16). `_workmeta` 아래에 쓰기 전 `npm run guard:workmeta-write -- --assert-write-target "<target>"`.
5. private 원문·사업명 세부·개인명·절대경로·secret은 public 파일에 넣지 않는다. 실행 결과는 수치와 상대 포인터만.
6. 병렬 코더를 쓰면: 패킷(설계 authority·baseline HEAD·입력·산출물·검증 명령·commit 메시지)을 문서로 주고, 격리 worktree, ff-merge 전 HEAD 검증. 결과 통합 시 계약 불일치는 컴파일러가 수용하고 시험으로 고정. 신선한 컨텍스트로 적대적 검토 1회.
7. 끝낼 때: 시험·validator 실제 결과 기록, `CHANGELOG.md`, 로드맵 delta log, 마스터플랜 CURRENT, 이 매뉴얼의 해당 장, five-field capture, commit+push, `규칙 강화 체크:`.

## 9.3 하지 말 것

- 규칙을 엔진 코드에 하드코딩하거나 컴파일러가 규칙을 "보완"하게 만들기.
- overlay로 evidence level 올리기·바꾸기(D45), 표준이 이미 요구하는 항목에 `add`.
- 어휘 밖 토큰을 스펙에 두고 넘어가기(unmapped context로 조용히 빠진다 — 시험이 잡는지 확인).
- 관측을 이웃 requirement로 추정해 붙이기, 미관측을 결손으로 접기.
- 실제 과제 실행 결과·원문을 public 문서·fixture에 넣기. Task 생성·승인·canon 승격·ERP write를 엔진이 하게 만들기.
- 새 top-level root·schema·workflow·정책 store를 Owner 계약 없이 만들기.

## 9.4 Owner에게 보고하는 법(이 프로젝트의 관행)

- 짧게, 한국어로, 비유 한 줄 + 표 하나. 다음 액션은 **하나**만 제시한다(긴 목록은 결정을 어렵게 한다).
- 매핑표·영수증 나열 대신 "무엇이 바뀌었고 무엇이 남았나"를 수치로. 확인 못 한 것은 UNKNOWN/HOLD로 정직하게.
