# .agent/catalog/identity/heroes/soulforge_default

## 목적

- `soulforge_default/` 는 `soulforge.species.default` 위에 얹을 수 있는 hero overlay candidate 를 둔다.
- candidate 는 direct character copy 가 아니라 normalized motif 또는 imprint 로만 기록한다.
