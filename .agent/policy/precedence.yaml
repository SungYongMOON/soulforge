policy_id: soulforge.policy.precedence
model: species_free_floor
layers:
  - layer_id: policy.floor
    rank: 10
    owner_section: policy
    description: species, class, workspace 이전에 적용되는 floor
  - layer_id: identity.species
    rank: 20
    owner_section: identity
    description: 현재 baseline 은 species only
  - layer_id: class.binding
    rank: 30
    owner_section: registry
    applies_when: active_class_binding_declared
  - layer_id: workspace.binding
    rank: 40
    owner_section: registry
    applies_when: active_workspace_binding_declared
  - layer_id: session.continuity
    rank: 50
    owner_section: sessions
    description: continuity 는 floor 를 보조하지만 override 하지 않음
