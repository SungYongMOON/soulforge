contract_id: soulforge.protocol.handoff
mode: continuity_only
required_fields:
  - handoff_id
  - from_actor
  - to_actor
  - checkpoint_ref
  - pending_work
  - referenced_files
  - open_questions
forbidden_fields:
  - raw_transcript
  - host_local_runtime_state
