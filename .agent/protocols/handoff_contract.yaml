contract_id: soulforge.protocol.handoff
mode: continuity_only
required_fields:
  - handoff_id
  - source_agent
  - target_agent
  - summary
  - decisions
  - open_risks
  - next_action
forbidden_fields:
  - raw_transcript
  - host_local_runtime_state
