# Repository Instructions

- Make minimal, scoped changes.
- Treat `.agent`, `.agent_class`, and `_workspaces` as separate layers with distinct responsibilities.
- Prefer documentation updates in `README.md` and `docs/architecture/` when architecture decisions change.
- Never commit local-only state from `.agent_class/_local/`.
