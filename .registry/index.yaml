version: phase1
owner: .registry
kind: outer_canon_store
status: active
paths:
  species: species/
  classes: classes/
  skills: skills/
  tools: tools/
  knowledge: knowledge/
  docs: docs/architecture/
notes:
  - `.registry` is now an active seeded canon surface, not a skeleton-only placeholder.
  - Species canon uses species/<species_id>/species.yaml with heroes inline.
  - Classes, skills, tools, and knowledge buckets may contain minimal seed entries that support unit, workflow, and execution examples.
