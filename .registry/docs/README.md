# .registry/docs

- `.registry/docs/` 는 `.registry` owner-local 문서 묶음이다.
- 현재 active 문서는 `architecture/` 아래에서 시작한다.
- skill install/sync 같은 운영 절차는 `operations/` 아래에서 설명한다.
- skill canon/executor boundary 는 `architecture/SKILL_CANON_BOUNDARY.md` 에서 고정한다.
