interface:
  display_name: "Soulforge GitHub Down"
  short_description: "Download/update Soulforge from GitHub."
  default_prompt: "Use $soulforge-github-down to run GitHub down through the registered latest-update workflow, including public/_workmeta/private-state freshness, skill sync, junction status, readiness checks, and blockers."

policy:
  allow_implicit_invocation: true
