interface:
  display_name: "Soulforge GitHub Down"
  short_description: "Update Soulforge and prepare this PC."
  default_prompt: "Use $soulforge-github-down to update Soulforge, inspect this PC's role and capabilities, and prepare the work it can safely perform."

policy:
  allow_implicit_invocation: true
