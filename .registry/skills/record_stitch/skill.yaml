skill_id: record_stitch
kind: skill
status: active
title: Record Stitch
summary: Turn confirmed fragments into a coherent structure without overreaching beyond evidence.
behavior:
  intent: Compose a stable structure from validated pieces.
  use_when:
    - source fragments are already sifted
    - an intermediate structure or map must be drafted
  avoid_when:
    - key relations remain unverified
  expected_outcome:
    - a coherent draft exists
    - speculative edges remain explicitly labeled
execution_requirements:
  required_capabilities:
    - structured_drafting
  preferred_capabilities:
    - evidence_review
  preferred_tools:
    - rg
    - markdown_editor
  notes:
    - actual model, attached skill package, and tool choice resolve in local runtime execution bindings.
notes:
  - `record_stitch` is the canonical skill behind `archivist.record_stitch`.
