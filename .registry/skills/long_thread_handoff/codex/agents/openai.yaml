interface:
  display_name: "Soulforge Long Thread Handoff"
  short_description: "Explicit fresh-worker phase handoff"
  default_prompt: "Use $soulforge-long-thread-handoff to move this long Soulforge task into its next phase with the current manager integrating fresh workers."

policy:
  allow_implicit_invocation: false
