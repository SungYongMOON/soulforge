# .registry/classes/knight

- `knight/` 는 canonical class sample 을 둔다.
- `class.yaml` 이 canon entry 이고 `*_refs.yaml` 이 assign/ref entrance 다.
- `skill_refs.yaml`, `tool_refs.yaml`, `knowledge_refs.yaml` 는 `.registry/skills`, `.registry/tools`, `.registry/knowledge` 의 actual canon entry 를 가리킨다.
