# demo_project

- `demo_project/` 는 tracked workspace contract example 이다.
- 실제 runtime site 가 아니라 문서화된 example set 이다.
- `_workmeta/` 아래에는 split binding set, workflow execution demo, owner-facing report example 같은 public-safe sample 문서만 남긴다.
