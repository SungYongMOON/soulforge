# autohunt

- 이 경로는 tracked auto-hunt policy example 만 둔다.
- 여기의 sample 은 mailbox routing, monster classification, workflow-party selection 규칙만 보여준다.
- queue 상태, retry counter, pending monster list, raw run truth 는 local `_workmeta/<project_code>/autohunt/` 와 `runs/<run_id>/` 아래에만 존재한다.

