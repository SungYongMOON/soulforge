# gateway mailbox example

- 이 경로는 `guild-hall:gateway:fetch` 가 만드는 mailbox raw/events/attachments 의 public-safe mirror sample 이다.
- 실제 운영 데이터는 `guild_hall/state/gateway/mailbox/**` 에만 존재해야 한다.
- 다른 PC 나 다른 LLM 은 여기 있는 sample 로 mailbox event shape 를 먼저 본다.
