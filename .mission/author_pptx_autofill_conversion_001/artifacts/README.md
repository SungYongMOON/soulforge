# artifacts

- `artifacts/` 는 mission-level public-safe artifact index 또는 release-facing material 을 둘 수 있는 reserved surface 다.
- project-local generated binaries 와 raw validation output 는 `_workspaces/<project_code>/.project_agent/runs/<run_id>/` 아래에 남긴다.
