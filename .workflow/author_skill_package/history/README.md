# .workflow/author_skill_package/history

- `history/` 는 skill package authoring lane 에서 public-safe 로 남길 수 있는 curated lessons 만 둔다.
- raw authoring transcript, local install logs, project-specific smoke check dumps 는 `_workspaces/<project_code>/.project_agent/runs/<run_id>/` 아래에 남긴다.
