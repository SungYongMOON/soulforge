# report_authoring_v0 calibrations

No CLI model-cost calibration has been run for this workflow. Pilot evidence is
fresh-context evaluator/judge runs on public-safe synthetic scenarios (see
`profile_policy.yaml`). If a model profile lock is wanted later, add a
quality-equivalence calibration run here and keep payloads public-safe.
