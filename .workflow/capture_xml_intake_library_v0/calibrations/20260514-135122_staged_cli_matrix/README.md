# 20260514-135122 staged CLI calibration

Public-safe staged CLI calibration for `capture_xml_intake_library_v0`.

This archive uses a synthetic Capture `EXP.xml` excerpt with invented component, net, connector, power, and no-connect facts. It does not contain real project XML, runtime absolute paths, `_workspaces` output data, `_workmeta` raw truth, credentials, cookies, private-state material, or secret-derived content.

The default `subagent_quality_first` path was unavailable under the current runtime policy, so this archive is labeled `cli_only_calibration`. Quality was evaluated by frozen criteria, deterministic rule evaluation, and Stage C finalist reruns. CLI telemetry is exact for these CLI runs only.

Recommendation: `gpt-5.4 | medium | elf | administrator`.
