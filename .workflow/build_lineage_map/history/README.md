# .workflow/build_lineage_map/history

- `history/` 는 계통도 workflow 에서 반복적으로 남길 curated lessons 만 둔다.
- raw execution transcript 와 intermediate artifact truth 는 `_workspaces/<project_code>/.project_agent/runs/<run_id>/` 가 소유한다.
