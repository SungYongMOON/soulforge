# .workflow/build_lineage_map/history

- `history/` 는 계통도 workflow 에서 반복적으로 남길 curated lessons 만 둔다.
- raw execution transcript와 intermediate artifact truth는 workspace/worksite가 소유하고, `_workmeta/<project_code>/runs/<run_id>/`에는 compact receipt만 둔다.

