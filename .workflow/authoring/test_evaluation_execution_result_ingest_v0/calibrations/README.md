# Calibrations

No calibration has been run for this draft package.

Future calibration archives must be public-safe and must not include private test logs, raw result payloads, waveform files, runtime absolute paths, credentials, sessions, or owner-only acceptance basis.
