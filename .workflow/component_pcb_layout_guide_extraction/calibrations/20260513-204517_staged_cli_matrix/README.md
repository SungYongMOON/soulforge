# 20260513-204517 staged CLI calibration

Public-safe staged calibration for `component_pcb_layout_guide_extraction`.

This archive uses a synthetic component-material fixture with mocked checksums, page spans, supplemental-source metadata, and visual/table candidates. It does not contain real PDFs, copied vendor text, project-local outputs, `_workspaces` run data, credentials, cookies, or downloaded binaries.

The default `subagent_quality_first` path was unavailable in this runtime, so this archive is labeled `cli_only_calibration`. Quality was evaluated by frozen criteria, rule evaluation, and semantic shortlist review. CLI telemetry is exact for these CLI runs only.

Recommendation: `gpt-5.4-mini | medium | elf | archivist`.
