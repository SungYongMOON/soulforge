# History

Curated lessons from source-bound layout extraction runs may be summarized here when they are public-safe. Raw runtime evidence belongs in the workspace/worksite; `_workmeta/<project_code>/runs/<run_id>/` keeps only compact receipts.
