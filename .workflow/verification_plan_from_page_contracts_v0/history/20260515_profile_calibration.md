# Profile Calibration - 2026-05-15

Calibrated `verification_plan_from_page_contracts_v0` against a public-safe synthetic fixture covering source-backed quantitative planning, missing criterion/source evidence, review-required harness state, source conflict, missing simulation model/deck, draft configuration baseline, and an unrelated owner decision.

Selected primary profile:

- `model`: `gpt-5.4`
- `reasoning_effort`: `low`
- `species`: `human`
- `class`: `auditor`

The primary was the lowest-cost quality-passing candidate. `gpt-5.4 / medium / human / auditor` is retained as the same-model quality shadow, and `gpt-5.5 / low / human / auditor` is retained as the high-stakes escalation shadow.

Boundary note: this calibration used only public-safe synthetic fixture data. It did not use raw project payloads, `_workspaces` material, private run truth, credentials, model payloads, simulation outputs, test logs, or runtime absolute paths.
