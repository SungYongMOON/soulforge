# Evaluator-Only Baseline Summary

The quality anchor classified all eight requested component/model-family pairs. U1 LTspice and U1 demo-circuit evidence are official source-index candidates only; no raw model or demo payload is acquired, runnable, or packaged by this workflow. U1 PSpice is missing because LTspice evidence cannot be converted or generalized.

J1 S-parameter and IBIS requests are blocked/access-gated because the fixture states that the authoritative page names SI assets but requires login and export-control acknowledgement. M1 PSpice, SIMPLIS, and demo-circuit requests are missing because typical curves are not model or demo-circuit evidence.

Compatibility must remain basis-labeled and conservative: no smoke-test basis is available, no cross-simulator conversion is authorized, and no downstream deck, simulator run, waveform, or pass/fail claim may be produced.
