# 20260514-171147_staged_cli_real_sample_structural

Public-safe staged CLI calibration for `whole_xml_page_split_v0` using structural metadata derived from the user-supplied real sample XML.

The archive intentionally stores no raw XML bodies, no generated page XML payloads, no host-specific runtime paths, no `_workspaces` outputs, no `_workmeta` raw truth, and no credentials or private-state material. The default `subagent_quality_first` path is recorded as blocked under the current runtime policy, so this calibration is explicitly labeled `cli_only_calibration`.
