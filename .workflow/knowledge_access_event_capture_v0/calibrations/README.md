# calibrations

No profile calibration has been performed.

Future calibration archives may be stored here only when fixtures and outputs are
public-safe. Do not store private knowledge payloads, copied source text,
advisory answers, credentials, project-local run truth, or runtime absolute paths.
