# Candidate Outputs

Candidate output bodies were reviewed from isolated subagent runs and summarized in `../candidates.jsonl`.

This calibration archive intentionally keeps only public-safe summaries in the tracked workflow canon. The fixture is synthetic; no raw project payload, private run truth, `_workspaces` output, credential, cookie, or session material belongs here.
