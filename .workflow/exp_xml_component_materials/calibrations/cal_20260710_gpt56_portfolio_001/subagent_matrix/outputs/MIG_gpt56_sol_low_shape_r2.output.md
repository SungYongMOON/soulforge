ASSUMPTIONS

You want me to execute the calibration replay prompt.

The public-safe fixture and mocked official-source catalog are missing, so I cannot produce the required evidence-grounded output without inventing data. Please provide those inputs; I’ll return exactly these fields:

`profile`, `intake_context_note`, `component_inventory`, `source_discovery_packet`, `download_manifest`, `downloaded_file_checksum_manifest`, `circuit_design_review_queue`, `boundary_review_note`, `circuit_design_readiness_note`.
