# Public tracked replacement candidate only. The installed automation is not
# updated by this file and remains outside this package's write authority.
#
# READ-ONLY INSTALLED ROLLBACK SNAPSHOT (observed 2026-07-30; verbatim fields)
# version = 1
# id = "soulforge-five-field-sweep"
# kind = "cron"
# name = "Soulforge 5-Field Sweep (daily)"
# prompt = "C:\\Soulforge 를 기준 루트로 사용한다(5필드 레저 정본: C:\\Soulforge\\_workmeta). 비밀값·메일 원문·첨부 본문은 읽지 않는다. 코드 수정, 서버 재시작, public repo push 는 하지 않는다.\n\n하루 1회 bounded 5필드 sweep 을 수행한다:\n0. 먼저 두 저장소를 최신화한다: git -C C:\\Soulforge pull --ff-only 와 git -C C:\\Soulforge\\_workmeta pull --rebase — 다른 PC 에서 작업된 커밋까지 sweep 대상에 포함시키기 위함. 충돌/비FF 면 중단·보고.\n1. node .workflow/five_field_session_capture_v0/tools/five_field_promotion_scan.mjs --report --repo-root C:\\Soulforge 를 실행해 승격 신호 리포트를 갱신한다.\n2. 갭 소급: 최근 24시간 커밋(C:\\Soulforge main 과 C:\\Soulforge\\_workmeta, git log --since=24.hours --pretty 와 --stat 요약만, 파일 본문 불필요)을 레저(five_field_log.jsonl 전 과제)의 같은 시간창 행과 대조한다. 레저에 대응 기록이 없는 작업 묶음마다 .workflow/five_field_session_capture_v0/tools/five_field_capture.mjs 로 소급 기록을 남긴다: --session-ref retro_<YYYYMMDD>_<짧은해시> --worker <커밋 표기 codex_*/claude_*, 없으면 unknown_ai> --data-label ai_backfill --request-kind <행위/출처 슬러그 추정>. judgment/output/verification 은 커밋 메시지와 diff --stat 요약에서만 도출한다(원문 미복사, input_refs 는 commit:<hash> 포인터).\n3. 드레인 지표 확인: needs_backfill 행 수, dev_worker_candidate_queue 상태 분포(proposed 적체 경고), 최근 24시간 레저 신규 행 수.\n4. _workmeta 변경분(레저·리포트)은 pull --rebase 후 작업자 표기(codex_<model>)로 commit+push 한다. 충돌 시 중단하고 보고한다.\n5. 짧은 요약 보고: 소급 N건, 승격 후보 목록, 드레인 지표 3개, blocker."
# status = "ACTIVE"
# rrule = "FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR,SA,SU;BYHOUR=7;BYMINUTE=35;BYSECOND=0"
# model = "gpt-5.3-codex"
# reasoning_effort = "medium"
# execution_environment = "local"
# target = { type = "project", project_id = "local-1018ddaeecab99443b06b78022cb14b7" }
# cwds = ["C:\\Soulforge"]
# created_at = 1783212029444
# updated_at = 1783212029444
#
# Rollback authority: human Owner only. Restore the saved complete installed
# TOML and the pre-run cursor record. Never delete or rewrite an append-only
# ledger commit that already reached the remote.

version = 1
id = "soulforge-five-field-sweep"
kind = "cron"
name = "Soulforge 5-Field Sweep (daily)"
prompt = "[PHASE C PUBLIC CANDIDATE / LIVE HOLD / PAUSED] Do not activate or run this tracked candidate without the immediately preceding human Owner approval of the exact first-live packet. The installed legacy automation remains ACTIVE and unchanged; this file neither updates nor runs it.\n\nRequired exact Owner-injected input: public {repo, ref, source_lane}, ratified seed and approved target, immutable public source snapshot; isolated ledger_writer clone/remote/ref/append-only ledger path for writer-workmeta; separate isolated cursor_writer clone/remote/ref for writer-private-state and cursor path guild_hall/state/operations/ai_work_result_recovery/v1/cursors/<source_lane_digest>.json; isolation.runtime_root; exclusive lease/lock root and recovery policy; exact source/writer/forbidden-root allowlists; installed runtime identity metadata; strict UTC recorded_at. Reject _workmeta/system/bindings/** as cursor ownership. No live binding value is hardcoded by this candidate.\n\nRun exactly one bounded transaction:\nnode .workflow/five_field_session_capture_v0/tools/five_field_cursor_runner.mjs --input <owner-approved-input.json>\n\nRequire canonical realpath containment. Reject equality, nesting, mutual overlap, reparse/symlink escape, and any source/writer/runtime/lock root within active C:\\Soulforge, active _workmeta, every Owner-supplied Codex/Orca worktree, or the installed-automation control root. The configured cwd is only the tracked runner code location and is never an approved source or writer surface.\n\nUse exact-key recursive validation. HOLD every unknown nested key, raw/chat/payload/body/messages/transcript or credential/token/password/cookie/session key, secret-shaped value, and absolute/private path, URL, or ref. Never echo a rejected envelope, private path, remote URL, or private ref in the receipt.\n\nAcquire an exclusive local lease with owner token, PID, host identity, acquired/expiry times, and monotonic writer_epoch. A live owner always blocks. Recover a stale lease only for same-host dead PID plus expiry and explicit Owner policy. Recheck the lease token and epoch immediately before ledger push, cursor commit, and cursor push; stale epoch cannot push either repository. Cursor sequence and writer_epoch are monotonic.\n\nProcess (seed, target] oldest-to-newest, with itemized exact self-loop exclusion and stable identity/full-record digest comparison. First validate and append the result ledger in writer-workmeta, commit, non-force push, fetch, and prove remote containment. Only then CAS the private-state cursor tuple, last commit, sequence, and writer_epoch, commit, non-force push, fetch, and verify remote containment plus persisted cursor content. Existing ledger records are never deleted or rewritten. Resume duplicates after failure. Reconcile an uncertain cursor push as UNKNOWN_AFTER_PUSH by a fresh remote read; never claim unchanged after an unverified push attempt.\n\nReport only the redacted operational receipt with official_completion=false, worksession_acceptance=false, taskdriver_acceptance=false, erp_acceptance=false, mcp_acceptance=false, and claim_ceiling=operational_evidence_only. Include baseline-to-target counts, cursor sequence/epoch before and after, digests, both repository publication proofs, lease checks, itemized exclusions, and active-tree mutations 0. The count 313 is the intentionally empty-ledger synthetic upper bound, never an actual missing count.\n\nSTOP on any boundary, lease, epoch, CAS, identity/digest, source/ref, non-FF, inclusion, runtime-identity, or redaction failure. STOP before any actual runtime root, private-state cursor, _workmeta ledger, private commit/push, installed automation update/activation, scheduler, network remote, ERP, or MCP action unless the human Owner approved those exact values immediately beforehand and a fresh independent Level 3 verifier accepted the exact diff and packet."
status = "PAUSED"
rrule = "FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR,SA,SU;BYHOUR=7;BYMINUTE=35;BYSECOND=0"
model = "gpt-5.3-codex"
reasoning_effort = "medium"
execution_environment = "local"
target = { type = "project", project_id = "local-1018ddaeecab99443b06b78022cb14b7" }
cwds = ["C:\\Soulforge"]
created_at = 1783212029444
updated_at = 1783212029444
