# Builder-only public template. This file is deliberately non-installable:
# it contains no live cwd, command, input path, target binding, or rollback
# bytes. Use five_field_automation_builder.mjs with Owner-injected current
# bytes and a PASS runtime-preflight binding to create an exact PAUSED
# candidate and byte-exact rollback pair in memory. Installation, execution,
# scheduling, activation, and rollback remain outside this file's authority.
version = 1
id = "soulforge-five-field-sweep"
kind = "cron"
name = "AI 작업 결과 누락 복구 (매일)"
prompt = "[PHASE E BUILDER-ONLY TEMPLATE / LIVE HOLD] This tracked file is not an installable automation definition. It contains no runnable cwd or command. Generate an exact PAUSED candidate and byte-exact rollback pair only through five_field_automation_builder.mjs using Owner-injected current bytes, absolute isolated paths, and a PASS runtime-preflight manifest. Do not install, run, schedule, activate, or roll back from this template. Preserve official_completion=false, worksession_acceptance=false, taskdriver_acceptance=false, erp_acceptance=false, mcp_acceptance=false, and claim_ceiling=operational_evidence_only."
status = "PAUSED"
rrule = "FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR,SA,SU;BYHOUR=7;BYMINUTE=35;BYSECOND=0"
model = "gpt-5.3-codex"
reasoning_effort = "medium"
execution_environment = "local"
target = { type = "project", project_id = "OWNER_INJECTED_AT_BUILD_TIME" }
cwds = []
created_at = 0
updated_at = 0
