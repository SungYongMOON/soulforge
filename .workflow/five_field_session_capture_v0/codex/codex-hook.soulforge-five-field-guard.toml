# Historical installation surface retained in a non-executable state.
# five_field_session_capture_v0 PostToolUse/Stop hooks are disabled.
# Reactivation requires owner approval and validation against the current Codex hook schema.
