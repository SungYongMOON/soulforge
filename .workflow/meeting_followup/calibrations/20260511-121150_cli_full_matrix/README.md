# 20260511-121150_cli_full_matrix

This archive stores the earlier public-safe CLI full matrix under the `meeting_followup` workflow.

- Candidate outputs: `cli_full_matrix/outputs/`
- Full 300-row output JSONL: `cli_full_matrix/results.jsonl`
- CLI telemetry: `cli_telemetry_probe/telemetry.jsonl`
- Rule and LLM evaluation: `evaluation/`
- Recommendation: `recommendation.yaml`

This is a legacy CLI import, not a subagent quality matrix. It is retained because token-heavy calibration runs should stay attached to the workflow they calibrate.
