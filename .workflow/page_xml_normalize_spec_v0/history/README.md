# History

Public-safe workflow history notes for `page_xml_normalize_spec_v0` may be stored here after bounded runs or calibrations.

History notes should summarize reusable procedure changes and validation results without embedding raw XML, generated sidecars, derived XML review variants, runtime absolute paths, project-local output payloads, credentials, cookies, or secret material.
