# page_xml_normalize_spec_v0 repeat Top-K contract refresh calibration

Public-safe repeat calibration for the refreshed `page_xml_normalize_spec_v0` workflow contract.

Scope:
- Reused public-safe structural fixture only.
- No raw XML, runtime absolute paths, credentials, private project payloads, or `_workspaces` material.
- Isolated subagent quality matrix over saved Top-K profiles from the prior policy.
- CLI telemetry probes only for quality-passing candidates.

Result: keep `gpt-5.4 / medium / elf / auditor` as primary.
