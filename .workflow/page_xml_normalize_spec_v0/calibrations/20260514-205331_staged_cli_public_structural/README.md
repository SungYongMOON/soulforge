# 20260514-205331_staged_cli_public_structural

Public-safe staged CLI calibration for `page_xml_normalize_spec_v0` using structural metadata from the already public-safe `whole_xml_page_split_v0` calibration archive.

The archive stores no raw XML bodies, generated page XML payloads, host-specific runtime paths, `_workspaces` outputs, `_workmeta` raw truth, credentials, cookies, or private-state material. The default `subagent_quality_first` path is recorded as blocked under the current runtime policy, so this calibration is explicitly labeled `cli_only_calibration`.
