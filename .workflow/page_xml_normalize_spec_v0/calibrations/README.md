# Calibrations

Public-safe calibration archives for `page_xml_normalize_spec_v0` may be stored here after the workflow is registered.

Archives must use synthetic or redacted fixtures only. Real page XML bodies, generated sidecars, derived XML review variants, runtime paths, project-local payloads, credentials, cookies, and private run truth do not belong here.
