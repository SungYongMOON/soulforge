# vanguard_strike

- `vanguard_strike/` 는 assault-oriented reusable party template 이다.
- 이 party 는 `frontline_assault` 를 기본 workflow 로 보고, `leader` slot 을 `vanguard_01` 에 앵커링한다.
- workflow step 순서는 `.workflow/` 가 소유하고, 이 폴더는 team composition 과 routing hint 만 소유한다.
