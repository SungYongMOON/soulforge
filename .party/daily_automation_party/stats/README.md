# daily_automation_party stats

Calibration and runtime stats are not available for this registered party.

When a controlled pilot runs, keep private execution truth under `_workmeta` or
the owning runtime state surface. Public stats here may contain only
metadata-only, public-safe summaries.
