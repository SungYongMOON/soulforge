# .party/guild_master_cell/stats

- `stats/` 는 guild-master authoring lane 에 대한 template-level observation note 만 둔다.
- project-local authoring run metrics 와 promotion truth 는 `_workspaces/<project_code>/.project_agent/runs/<run_id>/` 아래에 남긴다.
