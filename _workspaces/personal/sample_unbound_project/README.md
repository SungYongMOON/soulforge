# Sample Unbound Project

이 프로젝트는 Soulforge의 첫 unbound reference sample project 다.
운영용 실프로젝트가 아니라 workspace 상태판의 `unbound` 분류를 검증하기 위한 baseline 이다.

`.project_agent/` 는 의도적으로 만들지 않는다.
숨은 계약 파일이나 우회용 연결 메타도 두지 않는다.
