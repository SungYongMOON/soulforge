@AGENTS.md

## Claude 작업 규율 — 부분 관찰을 관찰로 치지 않기

실행 계약의 `관찰하지 않은 상태를 주장하지 않는다`는 **아무것도 안 본 경우**를 막는다.
아래는 **뭔가를 보긴 봤는데 그것만으로 단정한 경우**를 막는다. 2026-09-09 밤 한 세션에서
같은 모양의 오류가 다섯 번 났고, 다섯 번 다 답이 이미 열어본 폴더 안이나 한 명령 거리에 있었다.

- **증상은 선언이 아니다.** 부재·완결·상태를 주장하기 전에 그것을 선언하는 파일을 직접 읽는다.
  lane이 살아 있는지는 실패 로그가 아니라 그 lane의 `activation`/`feature_state`가 말한다.
  경로를 바꿔도 되는지는 그 binding의 `forbidden_roots`가, 무엇이 어디 있어야 하는지는
  `AGENTS.md`가 말한다. 실패하는 예약작업·빈 폴더·멈춘 수집은 **원인이 아니라 증상**이다.

- **닫힌 주장에는 찾은 범위를 함께 적는다.** "없다 / 둘뿐이다 / 전부다"를 쓸 때는 무엇을
  어디까지 찾았는지 같은 문장에 적는다. 범위를 적을 수 없으면 그 주장은 `UNKNOWN`이다.
  한 확장자·몇 단계만 훑고 전수를 주장하지 않는다.

- **기억이 관측과 맞는 것은 확인이 아니다.** 둘 다 같은 시점에 낡을 수 있다. 저장된 기억을
  근거로 쓰기 전에 그 대상의 **현재 선언을 다시 읽고**, 기억이 틀렸으면 그 자리에서 고친다.

- **고정 값이 박힌 것을 바꾸기 전에 박힌 곳을 전부 열거한다.** digest·sha256·절대경로가
  여러 곳에 복제돼 있을 수 있다(예약작업 인자, 정책 파일, 실행 스크립트). 목록을 먼저
  보고하고 나서 바꾼다. 하나라도 못 찾았으면 바꾸지 않는다.

- **돌고 있는 것을 멈추는 변경은 되돌리기 지점을 먼저 만든다.** 원본 사본을 남기고, 되돌리는
  명령을 실제로 적어둔 뒤에 바꾼다. 실패하면 원인을 더 파기 전에 **먼저 되돌려 서비스를
  복구**한다.

- **제안 전에 확인한다.** 사용자에게 방법을 제시하는 순간 그것은 이미 판단이다. "이렇게 하면
  됩니다"를 말하기 전에 그 방법을 막는 규칙이 있는지 먼저 읽는다. 제안했다가 정정하는 것은
  사용자에게 검사 부담을 넘기는 일이다.
